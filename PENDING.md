# 当前待处理列表

本文件只保存用户明确要求暂缓、以后集中处理的未完成事项。已完成或明确放弃的历史记录仅保存在本地 `dev` 分支，实施前无需加载归档文件。

## 工作规则

- 活跃状态只使用 `pending` 或 `planned`；完成后使用 `done`，明确放弃时使用 `dropped`，并迁入完成归档。
- 计划或实施新任务前先读取本文件，并核对未解决条目是否与当前任务有关。
- 若条目之间存在冲突或依赖，明确说明并请用户决定，不能静默遗漏。
- 记录条目不代表授权实现。
- 完成合并和验证并记录 PR、提交或版本证据后，才可标记为 `done`；完成记录仅保存在本地 `dev` 分支。
- `dropped` 必须记录用户的明确决定和原因；归档仅保存在本地 `dev` 分支。
- 新条目继续使用稳定递增 ID；下一个 ID 为 `AIO-PENDING-030`。

## 未解决条目

当前无未解决条目。
