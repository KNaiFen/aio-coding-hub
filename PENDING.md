# 当前待处理列表

本文件用于累计用户明确要求暂缓、以后集中处理的小问题。登记条目只保存需求与决策，不代表已经授权修改产品代码。

## 工作规则

- 状态使用 `pending`、`planned`、`done` 或 `dropped`。
- 进入正式计划模式，或用户明确要求开始修改时，必须先读取本文件，并把所有未解决条目加入当次候选处理清单。
- 若条目之间存在冲突、依赖或明显不适合在同一批交付，必须明确说明并请用户决定，不能静默遗漏。
- 条目进入实施后应关联 Trellis 任务；只有完成合并和验证后才能标记为 `done`。
- 已完成或放弃的条目保留在本文件中，并补充对应的提交、PR、版本或放弃原因。

## 条目

### AIO-PENDING-001：供应商开关应作为全局总开关

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-provider-routing-master-switch`
- 记录日期：2026-08-02
- 涉及范围：供应商管理、默认路由、自定义路由、Session 复用、首页/TUI 观测
- 当前问题：默认路由会检查供应商全局 `enabled`，但启用自定义路由模板后，候选选择和观测列表只检查该模板中的路由开关。结果是供应商界面显示“已停止”的供应商仍可能继续接收自定义路由流量，供应商开关并不是真正的全局总开关。
- 已锁定决策：供应商开关是全局总开关。供应商关闭后，任何路由都不能选择它；各路由中的对应开关必须变灰且不可操作。关闭发生时，已经发送到该供应商的当前上游尝试允许继续完成；从下一个尝试开始，该请求的重试、后续候选选择以及所有新请求都必须排除该供应商。
- 计划解决方向：
  1. 后端候选选择统一要求“供应商全局开启，并且当前路由开启”；默认路由、自定义路由、Session 复用和观测投影使用相同资格语义。
  2. 首页、TUI 和其他“首选/可用供应商”展示不得把全局关闭的供应商视为可用。
  3. 路由编辑界面在供应商全局关闭时禁用并灰显该供应商的路由开关，同时保留原来的路由开关值；重新全局开启后恢复原值和可操作状态。
  4. 全局开关变化继续清理相关 Session 路由缓存与运行时状态，避免旧绑定重新插入已关闭供应商。
  5. 更新当前明确允许“全局关闭但自定义路由继续使用”的回归测试，并增加默认路由、自定义路由、Session 复用、观测/TUI 和 UI 禁用态测试。
- 验收标准：
  - 全局关闭供应商后，新请求不会通过任何路由选择该供应商。
  - 自定义路由中的对应开关灰显、不可点击，且不会破坏已保存的路由内开关值。
  - 重新全局开启后，供应商按照各路由原先保存的开关和顺序恢复资格。
  - 首页和 TUI 的首选供应商、可用供应商列表与网关实际候选保持一致。
  - 全局关闭操作不影响已经建立的其他供应商配置，也不把熔断或额度状态与人工停用混为一类。
  - 全局关闭发生时，已经发出的当前上游尝试不会被强制中断；该尝试结束后的重试和切换不得重新选择已关闭供应商。
- 实施边界：关闭供应商不主动取消已经发出的网络请求，以免截断正常响应；该例外只覆盖当前在途尝试，不延伸到后续重试或新请求。

### AIO-PENDING-002：精简 TUI 面板顶栏

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-tui-observability-polish`
- 记录日期：2026-08-02
- 涉及范围：独立 TUI 的请求视图与供应商视图顶栏、窄终端布局
- 当前问题：供应商视图第一行的“供应商 2/2”和请求视图的“请求 1/2”只是两个视图的页码，并不是供应商可用比例或请求数量；供应商第二行的“供应商 10”则只是当前观测快照返回的供应商条目数。这些字段容易被误解，对日常观察价值较低并占用窄窗口空间。
- 已锁定决策：同时移除供应商视图的“供应商 2/2”、请求视图的“请求 1/2”以及供应商条目数“供应商 N”；左右键切换请求/供应商视图的交互保持不变，由视图标题和底部快捷键提示表达当前位置与切换方式。
- 计划解决方向：
  1. 请求视图和供应商视图第一行都不再生成视图页码，并重组相邻分隔符，避免留下空字段。
  2. 删除供应商视图第二行根据 `collection.items.len()` 生成的“供应商 N”数量，仅保留真正有操作价值的首选供应商、今日费用等摘要。
  3. 更新 TUI 渲染测试，覆盖供应商视图、请求视图和窄终端截断，避免删减后产生多余分隔符或空白，并防止未在本条目中决定的请求视图行为意外改变。
- 验收标准：
  - 请求视图不再显示“请求 1/2”，供应商视图不再显示“供应商 2/2”和“供应商 N”。
  - 窄窗口中首选供应商、今日费用等核心信息的可见空间不低于当前版本。
- 实施边界：仅删除上述三个顶栏显示项，不移除左右键切换、底部快捷键说明或供应商详情卡片。

### AIO-PENDING-003：精简 TUI 供应商消费限额行

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-tui-observability-polish`
- 记录日期：2026-08-02
- 涉及范围：独立 TUI 的供应商状态卡、消费限额摘要、窄终端布局
- 当前问题：供应商卡片使用不明确的“额”表示消费限额；没有配置限额时仍显示“额 未配置”，占用一整行；金额复用通用费用格式，小额会显示 6 位小数，例如 `$0.000000/$100.00`，影响扫描效率。
- 已锁定决策：将卡片标签“额”改为“限”；供应商没有任何消费限额窗口时不显示该行；已配置限额的当前用量和上限都固定显示 1 位小数。
- 计划解决方向：
  1. 供应商卡片仅在 `spend_windows` 非空时追加限额行；为空时不生成占位行，也不显示“未配置”。
  2. 将紧凑标签改为“限”，保留现有 `5h`、`日`、`月`窗口标签和窗口顺序。
  3. 为供应商限额摘要增加专用的一位小数美元格式，不修改首页今日费用、Statusline、请求费用或其他复用通用 `format_cost` 的位置。
  4. 更新卡片高度、分隔符、颜色和窄窗口测试，覆盖无配置、单窗口、多窗口及金额四舍五入。
- 验收标准：
  - 示例显示为 `限 5h $0.0/$100.0 日 $10.5/$200.0`。
  - 未配置消费限额的供应商卡片完全不显示限额行。
  - 已配置限额的当前用量和上限始终保留 1 位小数，并按正常四舍五入显示。
  - 隐藏限额行后，后续 OAuth 行和卡片分隔线正常上移，不留下空白行。
- 实施边界：本条目只锁定供应商列表卡片的紧凑限额摘要；供应商详情页中的“消费限额”说明和其他费用字段精度不在本次决定范围内。

### AIO-PENDING-004：在 TUI 供应商卡片显示账户余额

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-provider-account-usage-runtime`、`.trellis/tasks/08-02-tui-observability-polish`
- 记录日期：2026-08-02
- 涉及范围：供应商账户用量、后端观测快照、独立 TUI 供应商状态卡
- 当前问题：桌面供应商卡片可以查询并显示账户余额，但余额结果只保存在前端 React Query 缓存中；TUI 观测协议及供应商状态快照没有账户余额字段，因此无法显示同一结果。
- 已锁定决策：只有配置了账户余额查询的供应商才在 TUI 供应商卡片中显示紧凑的“余”摘要，视觉和排版与“限”行一致；未配置余额查询的供应商不显示该行。查询结果同时包含套餐剩余额度和现金余额时优先显示套餐剩余额度，缺少套餐剩余额度时才显示现金余额，并保留结果原单位。刷新频率继续使用供应商现有配置，不新增 TUI 独立频率设置。成功缓存自 `last_fetched_at` 起满 60 分钟即过期，不再展示旧值并必须在有活跃消费者时重新获取；重新获取失败或从未成功时显示 `余 获取失败`，不显示带“旧”标记的历史金额。
- 计划解决方向：
  1. 由后端维护不含凭据的最新账户用量结果，并向观测协议增加有界、可选的余额摘要；TUI 轮询观测快照时只读取缓存，不直接触发远端余额请求。
  2. TUI 仅在供应商配置了余额查询时追加“余”行；有效结果优先选择套餐剩余额度，否则选择现金余额，数值采用与紧凑“限”行一致的一位小数格式并保留原单位。加载中显示获取状态，缓存满 60 分钟或获取失败时不得继续展示历史金额。
  3. 桌面供应商卡片、手动刷新和 TUI 读取同一份后端最新结果，避免两个进程各自查询、互相覆盖或产生双倍请求。
  4. 余额查询、解析或观测投影失败必须 fail-open：只隐藏或降级“余”行，不能影响网关转发、路由、熔断或 TUI 其他内容。
- 验收标准：
  - 已配置并成功查询余额的供应商在 TUI 中显示类似 `余 $12.5` 的独立摘要行。
  - 同时存在套餐剩余额度和现金余额时展示套餐剩余额度；只有现金余额时才展示现金余额。
  - 未配置余额查询的供应商不显示“余”行，也不留下空白占位。
  - 成功结果未满 60 分钟时可以复用；满 60 分钟后视为过期并触发重新获取，重新获取失败或从未成功时显示 `余 获取失败`，不回退展示旧金额。
  - TUI 的高频界面刷新不会增加远端余额查询频率，实际查询严格复用 AIO 已保存的刷新设置。
  - 桌面面板与 TUI 最终显示来自同一次后端结果，金额、单位和更新时间一致。
  - 畸形、过期或未来版本的余额结果不会导致 TUI、观测接口或网关崩溃。
- 依赖与实施边界：依赖 `AIO-PENDING-005` 将定时刷新和结果缓存移到稳定的共享所有者；缓存过期只影响余额展示和重新获取，不影响供应商路由、熔断或网关请求。

### AIO-PENDING-005：建立供应商账户余额共享按需刷新

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-provider-account-usage-runtime`
- 记录日期：2026-08-02
- 涉及范围：供应商账户用量查询、定时调度、前端缓存、应用生命周期
- 根因复核：2026-08-02 已确认此前余额不刷新的直接原因是所有供应商都被全局关闭，当前前端查询把 `provider.enabled` 作为自动查询条件；“供应商已开启且页面保持焦点仍不刷新”的判断撤销，不再按该错误前提安排修复或回归测试。
- 当前问题：余额查询结果与定时器只属于单个 React Query observer，TUI 无法复用；同时全局路由开关错误地控制了余额查询。为了让桌面与 TUI 显示同一结果，需要建立稳定的共享缓存和按需调度所有者。
- 已锁定决策：余额定时刷新采用按需订阅。供应商页面正在展示账户用量，或至少一个 TUI 正在查看供应商视图时，按照供应商已保存的刷新间隔自动查询；两类消费者都不存在时停止定时远端请求，降低被供应商风控的风险。手动刷新继续立即生效。供应商即使被全局关闭，只要仍有上述活跃消费者且配置了余额查询，也继续按相同规则刷新余额；全局启停只控制路由资格，不控制账户余额查询。
- 计划解决方向：
  1. 将定时刷新从单个 React 组件生命周期迁移到后端共享的按需调度器；前端供应商页面通过挂载生命周期登记消费者，TUI 仅在使用 `include_providers` 轮询供应商视图时通过短期租约登记消费者。
  2. 第一个消费者出现时，若没有未满 60 分钟的成功缓存则立即异步查询；消费者存在期间按配置间隔刷新，最后一个消费者离开或租约超时后停止定时查询。可以保留最后一次无敏感字段结果供审计和短期复用，但满 60 分钟后不得继续作为可展示余额。
  3. 定时与手动刷新共享并发控制和结果时序，防止同一供应商重复请求、迟到旧结果覆盖新结果或一个慢供应商阻塞其他供应商。
  4. 远端失败只更新账户查询状态并按合理策略等待下个周期；如果旧成功结果已经满 60 分钟，则展示失败状态而不是旧金额。失败不重置或污染供应商路由熔断，不阻塞网关主链路，也不记录凭据和完整远端正文。
  5. 使用假时钟覆盖前端消费者挂载/卸载、TUI 供应商视图租约建立/续期/超时、间隔变更、手动与定时并发、失败恢复、60 分钟硬过期、全局关闭供应商继续刷新及多个供应商独立调度；不再增加基于错误根因的焦点刷新回归测试。
- 验收标准：
  - 设置 5 分钟且至少一个余额消费者活跃时，在不进行任何手动操作的情况下，每个到期周期都会产生一次对应供应商的余额查询并更新显示。
  - 仅供应商页面打开或仅 TUI 供应商视图连接时都能刷新；关闭供应商页面且没有 TUI 供应商视图订阅后，不再产生定时远端余额请求。
  - TUI 切回请求视图并超过租约宽限期后不再维持余额刷新；重新进入供应商视图时按缓存新鲜度决定是否立即查询。
  - 返回供应商页面时优先显示后端最后结果，并按缓存新鲜度决定是否刷新，不会因前端 `staleTime` 或旧缓存长期停留。
  - 同一供应商任一时刻最多一个余额查询生效，迟到结果不能覆盖更新的数据。
  - 全局关闭但仍有余额消费者的供应商继续按已保存间隔刷新；关闭消费者后仍按租约规则停止。
  - 查询失败和调度器异常不影响请求转发、路由选择、熔断状态或应用稳定性。
- 实施边界：全局启停只控制路由资格，不控制账户余额查询；余额仍只在至少一个桌面供应商页或 TUI 供应商视图消费者活跃时按需刷新，不变成无条件后台任务。

### AIO-PENDING-006：在 TUI 最近请求卡片显示输出速率

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-tui-observability-polish`
- 记录日期：2026-08-02
- 涉及范围：独立 TUI 请求记录卡、请求详情、输出速率格式化
- 当前问题：TUI 最近请求卡片已经显示路由结果和总耗时，例如“直连  8.9s”，但没有首页已有的输出 Token 速率；查看慢请求时无法快速区分等待首字和生成阶段的速度。
- 已锁定决策：在请求卡第 4 行的路由结果和耗时之后追加输出速率，例如 `直连  8.9s  52.6 t/s`；发生供应商切换或重试时继续保留现有聚合文案，例如 `切换2/重试3  31.4s  14.8 t/s`。
- 计划解决方向：
  1. 使用观测协议已有的输出 Token、总耗时和首字耗时在 TUI 端计算速率，不增加协议字段；计算和异常回退语义与桌面端 `computeOutputTokensPerSecond` 保持一致，包括上游缓冲导致生成窗口异常时回退到总耗时。
  2. 速率使用紧凑格式：小于 1000 时显示一位小数 `t/s`，达到 1000 后显示一位小数 `k t/s`。
  3. 缺少输出 Token、耗时或有效首字数据时不追加速率字段，避免错误请求和未完成请求出现没有意义的占位符。
  4. 保持现有 `切换N/重试M` 统计和 Enter 详情中的完整路由链；速率只补充卡片摘要，不取代尝试数、状态码或供应商信息。
- 验收标准：
  - 可计算速率的直连成功请求在耗时后显示输出速率。
  - 可计算速率的切换/重试请求同时显示聚合路由计数、耗时和速率，并在窄窗口中按现有截断规则安全收缩。
  - 无输出或无有效时序数据的失败、进行中和非流式请求不会显示误导性速率。
  - TUI 与首页对同一条请求计算出的速率和紧凑单位一致，并覆盖异常 TTFB、超高速率回退和千级缩写测试。
- 实施边界：保持请求卡片 5 行结构和现有颜色语义；本条目不增加新的路由链行，完整逐供应商失败详情继续通过 Enter 查看。

### AIO-PENDING-007：统一柔化 TUI 全局配色并精简最近供应商文案

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-tui-observability-polish`
- 记录日期：2026-08-02
- 涉及范围：独立 TUI Statusline、请求视图与详情、供应商视图与详情、终端颜色能力适配、最近供应商摘要
- 当前问题：AIO Statusline 的各字段直接使用 Ratatui 的 ANSI 基础色 `Cyan`、`Magenta`、`Green`、`Yellow` 和 `Red`，最终深浅取决于终端主题映射；请求模式和供应商模式的标题、卡片、状态、选中态、分隔符及详情字段也各自使用直接色值或基础 ANSI 色。当前没有像 Codex CLI 那样经过统一调色板柔化并按终端色彩能力降级，因此同一终端中不仅 Statusline 更深、更生硬，两个主页面也可能出现色调不一致或局部仍然过深。现有测试名虽然声称使用柔和 Codex 调色板，实际只断言了基础 ANSI 色，没有覆盖真正的柔化效果。另外，“近10 INPUT 大春×10”略显机械且乘号不符合用户期望。
- 已锁定决策：配色改进覆盖整个 TUI，而不只覆盖 Statusline。Statusline 各项目继续自动分配不同颜色；请求模式、供应商模式及其详情页统一使用同一套柔和、低刺激且接近 Codex CLI 观感的调色板。成功、失败、警告、进行中、不可用、未知、选中等语义仍保持稳定且清楚可辨，只调整色值、亮度、饱和度和终端降级方式，不改变业务状态含义。最近十次请求中占比最高的供应商摘要从 `近10 INPUT 大春×10` 改为 `最近 INPUT 大春 *10`，不可用和空值文案同步使用“最近”。
- 计划解决方向：
  1. 在 TUI crate 内建立单一共享调色板与语义颜色接口，Statusline、请求列表/详情、供应商列表/详情、顶部摘要、选中态、分隔线、底部帮助和新增可用性状态条都从该接口取色，不再在各渲染函数中散落基础 ANSI 色。
  2. 增加明确的终端颜色能力分层：TrueColor 使用经过柔化的 RGB 调色板，256 色终端映射到最接近的索引色，16 色或能力未知时使用可读的基础色降级；`--no-color` 行为保持不变。
  3. 颜色按语义类别稳定分配，避免所有字段退化为同一种蓝色；成功/高可用保持绿色，失败/低可用保持红色，警告和重试使用柔和暖色，进行中与选择焦点使用清晰强调色，未知/无数据与分隔符使用中性色。只对非正文辅助元素谨慎使用 `DIM`，不通过 `DIM` 柔化主要文字，以免部分终端进一步压暗。
  4. 请求卡片和供应商卡片的名称、状态、指标、分隔符及详情标签使用统一层级；相邻卡片仍能通过现有分隔或稳定的轻量强调色区分，但不得为了“多彩”牺牲状态语义和文字可读性。
  5. 调色板同时检查深色和浅色终端的对比度；如果无法可靠读取终端背景，则选择两套保守映射或提供确定性的降级，不依赖某个用户主题恰好如何解释 ANSI 基础色。
  6. 将最近供应商的正常、不可用和无记录三种文案统一改为 `最近 …`，正常计数使用 ASCII ` *N`，并更新窄窗口换行、截断、自定义 Statusline、请求/供应商主视图和详情页渲染测试。
- 验收标准：
  - 在用户当前终端中，AIO 与 Codex CLI Statusline 并排显示时，AIO 不再出现明显更深的整块青蓝/紫色文字，各语义字段仍容易区分。
  - 请求模式、供应商模式及两者详情页不再保留与新 Statusline 明显不一致的深色旧调色；标题、卡片、状态、选中态、分隔符和帮助文字属于同一套视觉体系。
  - 成功/高可用、失败/低可用、警告/重试、进行中、未知/无数据和选中焦点在请求页与供应商页保持相同语义，新增可用性状态条的绿、红、灰仍能直接区分。
  - TrueColor、256 色、16 色、未知能力和无颜色模式都有确定结果，不输出终端不支持的控制序列。
  - 默认示例显示为 `最近 INPUT 大春 *10`；不可用和无记录分别显示 `最近 不可用`、`最近 —`。
  - 窄窗口自动换行与截断不产生孤立分隔符，颜色样式不会改变可见宽度；`--no-color` 下所有信息、状态和选择焦点仍可通过文字或现有结构识别。
- 实施边界：不接入或复制 Codex CLI 完整语法高亮主题系统，也不改变请求、供应商或可用性状态的业务分类；正式实现时以轻量、共享且可测试的终端调色板覆盖整个 TUI。

### AIO-PENDING-008：在 TUI 供应商详情中手动测试可用性

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-provider-availability-core`、`.trellis/tasks/08-02-tui-observability-polish`
- 记录日期：2026-08-02
- 涉及范围：独立 TUI 供应商详情、本地观测动作接口、现有供应商可用性探测
- 当前问题：TUI 可以进入供应商详情查看路由、资格、熔断和额度信息，但本地观测服务目前只有只读的健康与快照 GET 接口，无法像桌面供应商页面一样主动执行可用性测试并查看结果。仓库已经存在统一的 `test_provider_availability` 领域能力，支持各 CLI、桥接供应商、测试模型、模型映射、OAuth/API Key 传输及有界响应预览，因此不应在 TUI 中另行拼装探测请求。
- 已锁定决策：在 TUI 供应商详情内提供手动可用性测试并返回结果；其行为必须与桌面 AIO 供应商页面点击“测试”完全一致，复用同一个领域函数、请求构造、测试模型、凭据解析、超时、成功判定和错误处理，TUI 只增加触发入口与终端结果展示。测试只能由用户显式触发，不做进入详情即测试、后台轮询或周期性测试，避免额外上游请求和风控风险。
- 计划解决方向：
  1. 在经过现有描述文件 Bearer Token 认证且仅监听回环地址的观测服务中增加受限 POST 动作，只接受有效供应商 ID，并直接调用现有领域探测函数；快照 GET 继续保持只读语义。
  2. 供应商详情使用 `t` 触发测试，底栏明确显示快捷键；触发后立即显示“测试中”，同一供应商测试未结束时忽略重复按键，但不阻塞 TUI 刷新、滚动、返回或退出。
  3. 结果区显示成功/失败、HTTP 状态、耗时、实际测试 Base URL 和简短错误；有界响应预览仅在存在时显示，并继续使用后端现有大小限制和敏感信息处理，不向 TUI 暴露 API Key、OAuth Token 或完整响应正文。
  4. 测试超时、AIO 关闭、认证过期、供应商被删除及未来协议不兼容均作为局部测试失败展示，不能让 TUI 崩溃，也不能影响网关转发、供应商路由、重试和余额刷新调度。
  5. 动作接口设置独立的并发限制和超时，不占满快照查询许可；测试结果只保存在当前 TUI 会话的详情状态中，切换供应商时按供应商 ID 隔离，避免迟到结果显示到错误卡片。
- 验收标准：
  - 在供应商列表按 Enter 进入详情后，按 `t` 可以看到明确的运行中状态，完成后原地显示测试结果。
  - 成功结果至少显示 `可用`、状态码和延迟；失败结果至少显示 `不可用`、可用的状态码及安全的简短原因。
  - 连续快速按 `t` 不会并发发送多个相同测试；切换视图或退出 TUI 不会留下失控的界面任务。
  - 桌面端与 TUI 对同一供应商产生相同的测试请求和可用/不可用判定，并复用相同的测试模型、模型映射、桥接传输、凭据、超时及错误语义；测试结果保持健康中立，不修改熔断计数或路由资格。
  - 非法 ID、未授权调用和非回环访问均被拒绝；任何测试错误不会终止 TUI 或 AIO 网关。
- 实施边界：源码已确认现有桌面探测函数不写熔断或路由状态；TUI 必须直接复用这一健康中立语义，不根据测试成功或失败自行修改供应商状态。本条目不增加自动或周期性可用性测试。

### AIO-PENDING-009：在桌面端与 TUI 展示供应商短期可用性状态条

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-provider-availability-core`、`.trellis/tasks/08-02-tui-observability-polish`、`.trellis/tasks/08-02-desktop-provider-availability`
- 记录日期：2026-08-02
- 涉及范围：供应商真实请求观测、手动可用性测试隔离、桌面供应商页面、独立 TUI 供应商页面、全局设置
- 参考界面：用户提供的示例采用“健康状态 + 成功/失败计数 + 一排离散时间单元”的紧凑结构；每个单元汇总一个时间段，并使用绿色、红色、灰色表达高可用、低可用和数据不足/不确定。
- 当前能力与缺口：仓库已有 `usage_availability_timeline_v1`，会从统一统计视图按供应商生成最多约 60 个时间桶，当前以 `2xx-3xx` 为成功，并在用量页每 15 秒刷新。不过该查询只使用请求的 `final_provider_id`：当 A 供应商失败、切换到 B 后成功时，只会给 B 记录最终成功，无法还原 A 的失败尝试。现有手动供应商测试结果不会持久化到时间线；根据后续锁定决定，这种隔离应继续保持。旧查询可复用时间边界和聚合思路，但不能直接作为本功能的事实来源。
- 已锁定决策：
  - 状态条同时加入桌面 AIO 供应商界面和 TUI 供应商界面。
  - 时间范围提供 `3 小时`、`6 小时`、`12 小时` 三档，在设置中全局选择，桌面端与 TUI 读取同一设置。
  - 常规数据只来自已经经过 AIO 的真实客户端请求，不允许 AIO 为维持状态条而自动或定时向供应商发送探测请求。
  - 此前曾考虑让用户手动点击供应商“测试”的结果参与统计；2026-08-02 后续决定明确覆盖该设想：手动测试结果不能作为短期可用性的判定依据，只在桌面端或 TUI 当前测试界面即时展示，不写入可用性观测，不影响成功/失败计数、成功率或红绿灰状态。
  - 每个时间单元只有绿、红、灰三种状态，不设置最低样本数：只要存在至少一个有效观测，就直接按成功率分类；成功率 `>= 75%` 为绿色，成功率 `< 75%` 为红色，只有完全没有有效观测时才为灰色，暂不增加黄色第四状态。
  - 桌面端保持现有 AIO 供应商页面的组件体系和视觉风格，优先复用已有组件；TUI 延续当前供应商面板的排版与颜色语言，不为本功能重新设计整体界面。
  - TUI 时间单元只通过绿色、红色和灰色区分，不要求使用不同字符；无颜色模式可以降级为中性状态条，成功/失败计数和详情文字仍可读取。
  - 桌面端每个时间单元支持鼠标悬浮详情，必须显示该单元对应的起止时间、成功次数和失败次数；总请求数、成功率及主要失败类别可在数据可靠且不造成信息拥挤时一并显示。
  - 桌面端固定显示 36 个时间单元：3 小时每格 5 分钟、6 小时每格 10 分钟、12 小时每格 20 分钟；TUI 固定显示 12 个时间单元：3 小时每格 15 分钟、6 小时每格 30 分钟、12 小时每格 60 分钟。时间单元按自然时钟边界对齐，TUI 每格聚合对应的 3 个桌面格。
  - 同一原始请求对同一供应商无论实际重试多少次，最多贡献一个观测：该供应商最终成功承接该请求时记一次成功；该供应商所有实际尝试均因供应商原因失败时记一次失败。例如 A 重试三次均失败后切到 B 成功，只记 A 失败一次、B 成功一次，避免重试策略放大单个请求的权重。
  - 只统计真实到达上游的请求。正常完成记成功；限流、上游超时、连接失败、鉴权错误、模型不支持以及其他由 AIO 归因给供应商的失败记失败。客户端取消、本地 JSON/请求校验错误、插件拒绝和未实际发送的跳过项全部忽略。
  - 桌面供应商页面在每张供应商卡片内部最下方新增独立的可用性栏，不与现有基础信息、余额或操作按钮挤在同一行。TUI 供应商卡按实际配置动态增减行数：“限”“余”“OAuth”各自在配置存在时显示，没有配置时不占行；可用性状态条始终位于卡片最后一行，不限制卡片必须为 5 行，全部配置时允许 6 行或 7 行。标准格式为类似 `6h ■■■■■■■■■■■■ 成24 败3` 的单行摘要，极窄窗口优先保留时间范围和 12 个状态单元并压缩计数文案，完整计数和分段详情通过 Enter 查看。
  - 短期可用性观测固定保留 24 小时，由后台每小时异步清理一次超过保留线的数据；清理失败只记录诊断并等待下个周期重试，不得阻塞网关、统计写入或桌面/TUI 展示。
- 计划解决方向：
  1. 建立后端统一的短期供应商可用性观测源，以每个真实客户端请求中实际到达上游的供应商尝试为基础记录有界字段；至少包含原始请求标识、时间、供应商、CLI 和成功判定，不保存请求正文、响应正文、凭据或大块路由 JSON。
  2. 在异步请求日志链路中按“原始请求 + 供应商”聚合各实际尝试，确保同一供应商的重复重试最多生成一个最终观测，“先失败后切换成功”会分别给失败供应商和成功供应商记入观测；本地拒绝、未到达上游的跳过项和客户端中断不得被误算为供应商失败。
  3. 手动测试继续复用 `AIO-PENDING-008` 的即时结果展示，但其完成事件不得进入短期可用性观测存储、聚合查询或前端/TUI 分类输入；即使测试返回 `ok`、HTTP 状态或错误，也只能用于该次测试结果本身。
  4. 后端返回统一的时间边界、原始成功/失败计数和可聚合观测，桌面端与 TUI 使用同一三态分类函数，避免两端对同一供应商显示不同颜色。
  5. 桌面供应商卡片在内部底部新增独立可用性栏，采用连续的紧凑矩形单元，并使用项目现有 Tooltip/Popover 组件展示单元详情，不依赖浏览器原生 `title`；TUI 将单行状态条放在动态卡片的最后一行，使用相同字符并仅用前景色或背景色表达三态，详情中显示范围、分段大小及汇总计数。
  6. 设置读取或分类异常必须 fail-open 为灰色/隐藏状态条，不能阻塞网关、改变路由、重试、熔断或供应商健康；统计写入继续异步执行。
  7. 可用性观测写入与请求转发解耦，记录保留 24 小时；后台清理任务每小时删除过期数据并采用有界事务，数据库忙或清理失败时延后重试，不在请求链路同步补删。
- 验收标准：
  - 桌面端与 TUI 能显示同一供应商过去 3/6/12 小时的绿、红、灰时间序列，并随全局设置同步切换范围。
  - A 失败后切到 B 成功时，同一时间段中 A 获得失败观测、B 获得成功观测，不会只记录最终 B。
  - 没有有效请求时为灰色；只要存在有效请求就不再使用灰色，成功率恰好 75% 时为绿色，低于 75% 时为红色。
  - 同一原始请求在同一供应商重试多次只贡献一个最终观测；全部失败后切换到其他供应商成功时，前者记一次失败、后者记一次成功。
  - 供应商原因导致的限流、超时、连接、鉴权和模型不支持错误计为失败；客户端取消、本地错误、插件拒绝和跳过项不进入任何时间单元。
  - 手动测试只在用户点击时发起，完成后不会进入任何供应商时间段，也不会改变状态条计数或颜色；状态条本身不会产生任何远端请求。
  - 桌面任一格悬浮后都能看到准确的起止时间、成功数和失败数；桌面悬浮信息和 TUI 详情显示的时间、成功数、失败数及分类一致。
  - 桌面状态条位于每张供应商卡片内部最下方的独立栏；TUI 状态条位于动态卡片最后一行，“限”“余”“OAuth”按配置独立显示且不留下空白行。
  - TUI 开启颜色时可直接区分三态，关闭颜色时仍能读取汇总和详情。
  - 任何超过 24 小时的观测会在下一次每小时清理周期中移除；清理延迟或失败不影响新观测写入和网关转发。
  - 观测解析、存储、聚合或渲染失败不影响请求转发和应用/TUI 稳定性。
- 实施边界：本条目的用户可见位置、时间范围、分类、计权和保留策略均已锁定；正式计划只需结合现有供应商卡片结构、数据库迁移和异步任务框架确定代码落点，不再引入新的产品规则。

### AIO-PENDING-010：在 macOS 顶栏图标悬停展示供应商可用性 mini 面板

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-macos-tray-provider-mini`
- 记录日期：2026-08-02
- 涉及范围：macOS 顶栏 Tray、供应商当前路由资格、短期可用性状态条、轻量悬浮窗口
- 当前能力与可行性：macOS 当前使用 Tauri `TrayIconBuilder` 创建顶栏图标，现有回调只处理左键 `Click`，左键会显示主窗口，右键则使用原生菜单提供“显示/隐藏”和“退出”。仓库锁定的 Tauri `2.9.5` 与 `tray-icon 0.21.3` 已在 macOS 实现 `Enter`、`Move` 和 `Leave` 事件，因此可以直接实现真正的鼠标悬停，而不必占用右键。系统原生 tooltip 只能显示简单文本，无法承载多行、彩色、动态的供应商状态条；本功能需要使用锚定在 Tray 图标下方的无边框 mini 窗口。
- 已锁定决策：
  - 功能首先只在 macOS 启用；其他平台保持现有 Tray 行为。
  - 鼠标移入顶栏图标时显示 mini 面板，移出图标及面板后关闭。左键继续打开 AIO 主窗口，右键继续显示现有原生菜单，不用右键替代悬停，也不移除“显示/隐藏、退出”。
  - 面板只展示当前生效路由模式中已加入且路由开关开启的供应商，每个供应商独占一排；全局关闭的供应商按照 `AIO-PENDING-001` 不属于可展示的启用供应商。
  - 每排使用 `AIO-PENDING-009` 的同一短期可用性事实来源、全局 3/6/12 小时范围、12 单元紧凑状态条和绿/红/灰分类；不复制另一套判定逻辑，也不因打开或悬停面板向供应商发起探测请求。
  - 面板采用紧凑 mini 形态，不抢键盘焦点、不激活 Dock、不显示为普通应用窗口，也不改变主窗口显示状态。
  - 多套 CLI 同时启用时，面板优先展示当前存在活跃请求且最近开始请求的 CLI；没有活跃请求时展示最近完成客户端请求所属的 CLI。一次悬停打开期间固定该 CLI，不因新请求到达而跳换内容，下次重新打开时再重新选择。若从未产生过可识别请求，则按现有 CLI 显示顺序选择第一个网关已开启的 CLI。面板顶部显示 CLI、当前路由名称和时间范围，例如 `Codex · 默认路由 · 6h`。
  - 当前路由开关和供应商全局开关均开启的供应商即使暂时熔断、冷却、消费限额或 OAuth 额度耗尽也继续显示；对应行增加紧凑的“熔”或“限”等不可用标记，使面板能够解释为什么该供应商当前不会被选择。
  - 面板最多同时显示 10 个供应商行；超过 10 个时保持同一固定高度并允许在面板内使用鼠标滚轮纵向滚动。鼠标位于可滚动面板内时视为仍在悬停区域，不自动关闭。
- 计划解决方向：
  1. 扩展现有 Tray 事件处理，响应 macOS `Enter`、`Move`、`Leave`，使用事件携带的图标矩形和当前屏幕工作区将 mini 窗口定位在图标下方；多显示器、刘海、安全区域和屏幕边缘下自动调整位置。
  2. 新增单实例、无边框、置顶、跳过任务栏且默认隐藏的轻量窗口；首次悬停后复用同一窗口，避免频繁创建 WebView。窗口内容只加载供应商 mini 面板，不加载首页完整布局。
  3. 图标与面板共同构成悬停区域；离开后使用短暂延迟关闭，并在重新进入时取消关闭，避免鼠标从图标移动到面板时闪烁。窗口失活、屏幕切换、Tray 被关闭或应用退出时保证隐藏并清理计时状态。
  4. 复用后端当前路由供应商投影和短期可用性聚合，在每次打开时按“活跃请求 → 最近完成请求 → 第一个已开启网关”的顺序选择并冻结 CLI。每排至少显示供应商名称和 12 单元状态条，可在宽度允许时追加成功/失败计数；熔断、冷却或达到消费/OAuth 限额的行继续保留并显示紧凑原因标记。
  5. 面板主体最多布局 10 个供应商行；更多行使用有界滚动容器和轻量的滚动位置提示，滚轮操作不请求焦点。关闭后滚动位置复位，下次打开从路由首位开始。
  6. 数据不可用、数据库忙、未来协议字段或窗口创建失败全部 fail-open：隐藏面板或显示中性不可用状态，只记录有界诊断，不影响 Tray 左右键、主窗口、网关转发、路由、熔断和余额刷新。
  7. 增加纯状态机与前端渲染测试，并由 GitHub Actions 的 macOS 原生任务验证 Tray 进入/离开、点击互不冲突、窗口锚定、焦点和生命周期；本机继续遵守不运行 Rust/native 构建的仓库规则。
- 验收标准：
  - macOS 鼠标移入 AIO 顶栏图标后，图标下方稳定出现 mini 面板；离开图标和面板后自动关闭，不频繁闪烁或遗留窗口。
  - 当前路由模式中每个已启用供应商各显示一排，顺序与该路由顺序一致；未加入路由、路由关闭或全局关闭的供应商不显示。
  - 有活跃请求时选择最近开始请求的 CLI，无活跃请求时选择最近完成请求的 CLI；当前打开期间不会切换 CLI，顶部能看到 CLI、路由名称和时间范围。
  - 熔断、冷却或额度耗尽但路由仍开启的供应商保留在列表中，并显示“熔/限”等原因；它们不会被错误显示成当前可路由供应商。
  - 供应商不超过 10 个时不出现滚动；超过 10 个时面板高度不继续增长，鼠标滚轮可以访问全部路由启用供应商，移入面板滚动时不会意外关闭。
  - 每排状态与桌面供应商卡片和 TUI 对同一供应商、同一时间范围的 12 单元聚合一致，悬停本身不会产生任何远端请求。
  - 左键仍打开主窗口，右键仍打开“显示/隐藏、退出”原生菜单，悬停面板不抢焦点、不显示 Dock 图标。
  - 多显示器和屏幕边缘下窗口保持在可视区域；应用退出、关闭 Tray 或禁用托盘常驻后不会残留 mini 窗口。
  - 面板数据或渲染失败不影响 AIO 核心转发能力和现有 Tray 操作。
- 实施边界：CLI 选择、暂时不可用供应商展示和超过 10 行后的滚动规则均已锁定；正式计划只需结合现有 Tray 生命周期、观察快照、路由名称来源和 mini 窗口能力确定代码落点，不再引入新的产品规则。

### AIO-PENDING-011：在 TUI 请求卡中前置供应商名称

- 状态：`planned`
- Trellis：`.trellis/tasks/08-02-tui-observability-polish`
- 记录日期：2026-08-02
- 涉及范围：独立 TUI 最近请求卡片、窄终端截断
- 当前问题：请求卡第 3 行当前按“工作目录  供应商”显示，例如 `aio-coding-hub  INPUT 大春`。不同目录长度会让供应商名称左右移动，难以快速扫描最近请求实际使用的供应商。
- 已锁定决策：第 3 行改为“供应商  工作目录”，标准示例为 `INPUT 大春  aio-coding-hub`；供应商必须位于左侧稳定位置，其他请求卡行和详情页字段顺序不变。
- 计划解决方向：在现有请求卡格式化边界交换供应商与目录顺序，继续使用两个空格分隔和既有 Unicode 宽度截断；缺少供应商或目录时保留现有安全占位。
- 验收标准：
  - 正常请求显示 `INPUT 大春  aio-coding-hub`，不再显示反向顺序。
  - 窄终端从右侧截断工作目录时仍优先保留供应商名称。
  - 缺少供应商或工作目录不会产生空行、孤立分隔符或崩溃。
- 实施边界：请求卡保持 5 行结构；本条目不改变观测协议、请求详情页或其他 Statusline 字段。

## 已完成或放弃

暂无。
