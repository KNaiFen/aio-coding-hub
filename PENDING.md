# 当前待处理列表

本文件只保存用户明确要求暂缓、以后集中处理的未完成事项。已完成或明确放弃的历史保存在 [`PENDING_COMPLETED.md`](./PENDING_COMPLETED.md)，实施前无需加载归档文件。

## 工作规则

- 活跃状态只使用 `pending` 或 `planned`；完成后使用 `done`，明确放弃时使用 `dropped`，并迁入完成归档。
- 进入正式计划模式，或用户明确要求开始修改时，必须先读取本文件，并把所有未解决条目加入当次候选处理清单。
- 若条目之间存在冲突、依赖或明显不适合在同一批交付，必须明确说明并请用户决定，不能静默遗漏。
- 记录条目不代表授权实现；进入实施后必须关联 Trellis 任务。
- 只有完成合并和验证并记录 PR、提交或版本证据后，才可标记为 `done` 并迁入 `PENDING_COMPLETED.md`。
- `dropped` 必须记录用户的明确决定和原因后再迁入归档。
- 新条目继续使用稳定递增 ID；下一个 ID 为 `AIO-PENDING-016`。

## 未解决条目

### AIO-PENDING-015：压缩并稳定 Tray 供应商悬浮小窗布局

- 状态：`planned`
- Trellis：`.trellis/tasks/08-03-tray-provider-mini-density`。
- 记录日期：2026-08-03
- 重新打开日期：2026-08-03
- 涉及范围：macOS Tray 供应商 mini 面板的前端列布局、原生窗口宽度、计数格式、既有行密度与回归测试。
- 当前问题：`aio-coding-hub-v0.60.45` 已把供应商行高从 `36px` 收紧到 `24px`，但用户提供的实际截图证明横向布局仍未达到验收。当前原生窗口固定为 `440px`，前端使用 `minmax(0,1fr) 216px 64px`；名称列会占用过多横向空间，成功/失败合计区又只有 `64px`。当计数增长到四位数时，`成1034`、`败33` 会在 flex 容器中被挤压换行，使“成”“败”和数字的位置随内容变化，整排失去稳定对齐。
- 已锁定决策：
  1. 保留已经交付的 `24px` 行高、18 格可用性条、不可用原因标记、十行滚动上限和标题栏结构。
  2. 供应商名称改为固定最大宽度，超过后单行省略；完整名称继续通过 `title` 暴露，不为短名称保留一整块可伸展空白。
  3. “成”和“败”必须分别位于固定列，成功数与失败数也各占固定数值列；数字长度变化不得推动标签、状态条或相邻行位置，不得换行。
  4. 常见计数保留完整数字；超过固定数值列容量时使用确定性的紧凑表示，并在 `title` 与无障碍文本中保留精确值。
  5. 面板整体同步收窄，但不改变圆角、毛玻璃、悬停开关、快照冻结、供应商筛选排序或 Tray 左右键行为。
- 计划解决方向：
  1. 将原生逻辑宽度从 `440px` 收紧到 `404px`，前端供应商行采用固定 `96px / 178px / 88px` 三列，分别承载名称与原因、18 格状态条、成功/失败合计；保留现有 `1px` 左右边框、`12px` 横向内边距和 `8px` 列间距。
  2. 名称列保持 `min-width: 0`、单行 `truncate` 和完整名称标题；原因标记仍在同一固定列内，不改变去重与颜色语义。
  3. 合计区改为 `12px / 32px / 12px / 32px` 四个固定轨道，标签固定、数值右对齐并使用 tabular numbers；五位以内显示原数，更大值以 `万/亿` 紧凑显示，禁止换行。
  4. 同步更新 Tray mini 跨层宽度合同、React 渲染测试和原生窗口 placement/scale 几何断言；Rust/native 测试继续仅由 GitHub Actions 执行。
  5. 使用可控 Tauri bridge fixture 在 `404px` 逻辑宽度下验证长名称、0/一位/四位/超大计数和多行混排，并保存普通与 2x 像素密度截图供视觉复核。
  6. 通过功能 PR 合入 `origin/main` 后，按补丁规则发布 `aio-coding-hub-v0.60.46`；精确 main SHA 候选 CI、Release 资产和校验和全部验证后再归档本条与父任务。
- 验收标准：
  - 面板逻辑宽度为 `404px`；与 `0.60.45` 的 `440px` 相比明显收紧，标题和列表均无右侧无意义留白、横向裁切或溢出。
  - 短名称、超长中文名称、超长英文名称及一至三个原因标记混排时，名称区宽度始终相同；超长名称单行省略且悬停可查看完整值。
  - `成`、成功数、`败`、失败数在所有供应商行使用完全相同的水平起点；计数从 `0` 增长到 `9`、`1034`、五位数及 `u32` 级大值时不换行、不挤压状态条、不推动任一标签。
  - 18 格状态条在每行保持固定宽度、等分和间距；名称、原因标记、状态格和计数均在 `24px` 行内清晰垂直居中。
  - 1、5、10 和超过 10 个供应商的高度、滚动回顶、悬停交接、CLI/路由冻结和空状态行为保持 `0.60.45` 既有语义。
  - 前端定向测试、源码范围完整测试、TypeScript、ESLint、Prettier、Vite build、规格链接和 Trellis 校验通过；GitHub Actions 的 Rust、几何、桌面构建与发布门禁通过。
- 实施边界：本条只修正 Tray mini 横向密度和稳定对齐，不改变请求计数口径、可用性聚合、供应商资格、路由、刷新节奏、快照协议或其他桌面页面。
