# 当前待处理列表

本文件只保存用户明确要求暂缓、以后集中处理的未完成事项。已完成或明确放弃的历史保存在 [`PENDING_COMPLETED.md`](./PENDING_COMPLETED.md)，实施前无需加载归档文件。

## 工作规则

- 活跃状态只使用 `pending` 或 `planned`；完成后使用 `done`，明确放弃时使用 `dropped`，并迁入完成归档。
- 进入正式计划模式，或用户明确要求开始修改时，必须先读取本文件，并把所有未解决条目加入当次候选处理清单。
- 若条目之间存在冲突、依赖或明显不适合在同一批交付，必须明确说明并请用户决定，不能静默遗漏。
- 记录条目不代表授权实现；进入实施后必须关联 Trellis 任务。
- 只有完成合并和验证并记录 PR、提交或版本证据后，才可标记为 `done` 并迁入 `PENDING_COMPLETED.md`。
- `dropped` 必须记录用户的明确决定和原因后再迁入归档。
- 新条目继续使用稳定递增 ID；下一个 ID 为 `AIO-PENDING-016`。

## 未解决条目

### AIO-PENDING-012：TUI 请求卡按模型路由分行展示

- 状态：`planned`
- Trellis：`.trellis/tasks/08-03-tui-request-card-model-route-lines`。
- 记录日期：2026-08-03
- 涉及范围：独立 TUI 请求列表卡、模型路由格式化、窄终端截断与 TUI 渲染测试。
- 当前问题：请求卡目前将已应用的模型路由显示为单行 `source→effective`。源模型、目标模型和附加信息组合后容易超出窄终端可用宽度，使目标模型被截断，不利于快速辨认实际路由结果。
- 已锁定决策：
  1. 只要实际模型发生变化（已应用的 `source_model != effective_model`），请求列表卡始终拆为两行模型信息，而不是仅在宽度不足时拆分。
  2. 第一行格式为 `CLI / 源模型 →`；第二行在当前卡片可用宽度内右对齐显示目标侧文本。
  3. 已应用的思考强度直接紧跟目标模型，以 `目标模型·high` 形式显示，不显示“思考”二字。
  4. 普通请求以及仅改变思考强度、未改变实际模型的请求继续使用单行模型信息。
  5. TUI Statusline 和 Enter 打开的请求详情页继续保持现有单行 `source→effective` 表示，不随本条改版。
  6. 请求卡允许根据内容动态增减行数；不再沿用历史上的固定五行约束。
- 计划解决方向：
  1. 增加仅供请求列表卡使用的模型路由格式化分支，不改变 Statusline 和详情页共用的单行格式化函数。
  2. 使用 grapheme/Unicode 显示宽度计算目标行右对齐与源、目标模型截断；极窄终端优先保留 CLI 前缀和路由箭头，绝不溢出、出现负填充或 panic。
  3. 保持已有上下文压缩标记语义，并将其与目标侧文本共同处理，避免额外增加无必要的信息行。
  4. 将请求卡颜色从固定行号推导改为能覆盖路由分行的语义位置，确保模型续行、供应商、路由耗时和指标的色彩不因插入行而错位。
  5. 更新本地观测 TUI 契约及格式化/渲染测试；Rust/TUI 验证由 GitHub Actions 执行。
- 验收标准：
  - 目标示例在有足够宽度时显示为：
    ```text
    Codex / gpt-5.6-sol →
                     gpt-5.6-terra
    ```
  - 带思考强度时显示为类似 `gpt-5.6-terra·high`，不出现“思考 high”或“思考high”。
  - 实际模型未变化的请求不新增模型续行；非路由请求的现有摘要信息不改变。
  - CJK、超长模型名、压缩标记和可用宽度极小时均安全截断，路由卡、普通卡混排时选中、滚动、分隔线与配色正确。
  - Statusline、请求详情、观测协议和网关路由行为不发生功能变化。
- 实施边界：本条只改变 TUI 请求列表的可读性，不改变模型选择、路由执行、请求日志存储或桌面端展示。

### AIO-PENDING-014：消除余额心跳刷新导致的周期性布局抖动

- 状态：`planned`
- Trellis：`.trellis/tasks/08-03-provider-account-usage-heartbeat-ui`。
- 记录日期：2026-08-03
- 涉及范围：桌面 Providers 页账户余额摘要、React Query 消费者心跳、手动刷新反馈与前端回归测试。
- 当前问题：已配置账户余额的供应商卡每五秒执行一次消费者心跳。当前组件把任何 `isFetching` 都渲染成“账户: 刷新中”，同时移除已有余额指标；心跳完成后又恢复原内容。即使后端只是返回缓存、没有执行远端余额刷新，所有已挂载卡片也会周期性改变文本和高度，造成整个 Providers 页面闪烁和布局抖动。
- 已确认根因：`useProviderAccountUsageQuery` 固定使用 `refetchInterval: 5_000` 续租后端消费者；`ProviderAccountUsageInline` 在 `isFetching` 时无条件用“账户: 刷新中”替换摘要、清空 metrics 并旋转刷新图标。前端将内部租约心跳错误地暴露成了用户可见的远端刷新状态。
- 已锁定决策：
  1. 五秒消费者心跳继续属于内部保活机制，不得再导致余额文本、指标行、卡片高度或周边页面布局发生可见变化。
  2. 保持既有余额正确性决定：真实查询结果为失败、过期或无有效金额时仍不展示旧余额；本条不能借消除闪烁之名回退该规则。
  3. 本条只处理稳定复现的桌面显示抖动，不把偶发的批量获取失败标记为已修复或已找到根因。
- 计划解决方向：
  1. 区分“首次且没有缓存结果的加载”“五秒后台心跳”和“用户手动强制刷新”，不再直接用通用 `isFetching` 决定整段显示内容。
  2. 已有有效结果时，后台心跳期间继续稳定渲染相同的余额摘要和指标；心跳本身不显示旋转动画、不禁用卡片，也不清空 metrics。
  3. 首次没有任何结果时可以显示“账户: 刷新中”。手动点击刷新时提供明确但不改变布局的反馈：保留当前摘要和指标，仅使用固定尺寸的刷新图标状态并防止重复点击。
  4. 后台或手动请求返回后再依据真实结果更新界面；成功时原位更新余额，失败时按既有策略切换到失败状态并隐藏旧金额。
  5. 保持五秒消费者租约、后端刷新间隔、共享运行时和 TUI 投影不变，避免用延长轮询或关闭后台刷新掩盖前端状态建模错误。
- 验收标准：
  - 余额首次加载成功后，即使 Providers 页面持续打开并经历多个五秒心跳周期，摘要、指标、卡片高度和相邻卡片位置都不发生周期性闪烁或位移。
  - 自动心跳不显示“账户: 刷新中”或旋转图标；只有首次无数据加载和用户显式手动刷新提供加载反馈。
  - 手动刷新期间保留已有内容和稳定尺寸，重复点击不会启动并发强制刷新；成功后原位更新为新结果。
  - 真实失败、过期和首次失败仍不显示旧余额，并保持现有错误文案与颜色语义。
  - 使用 fake timers 覆盖至少三个连续心跳周期，并覆盖多张供应商卡同时挂载、首次加载、手动刷新、后台成功更新和后台失败；测试必须证明 DOM 内容与布局相关结构在心跳期间保持稳定。
- 实施边界：本条是前端展示状态修复，不改变账户余额远端请求协议、凭据、后端调度、查询频率、TUI 行为或供应商路由。

### AIO-PENDING-015：压缩 Tray 供应商悬浮小窗的单行高度

- 状态：`planned`
- Trellis：`.trellis/tasks/08-03-tray-provider-mini-density`。
- 记录日期：2026-08-03
- 涉及范围：macOS Tray 供应商 mini 面板的前端行密度、原生窗口高度计算、十行滚动上限与回归测试。
- 当前问题：供应商列表目前为每行固定 `36px`，而行内最高的不可用原因标记为 `18px`，视觉上约留下上下各 `9px` 的空白；供应商较多时这些余量会累积，使悬浮小窗显得过于松散。前端同时将十行列表上限固定为 `360px`，原生窗口也使用 `36px/行` 计算实际高度，因此不能只缩小前端内容而不调整窗口几何。
- 已锁定决策：
  1. 明显压缩每个供应商行的上下留白，让列表更紧凑；仍保持一个供应商独占一行。
  2. 不通过删除供应商名称、不可用原因标记、18 单元可用性条或成功/失败计数来换取高度。
  3. 只调整供应商列表密度；标题栏、面板宽度、圆角与毛玻璃外观、悬停开关逻辑及空状态高度保持现状。
- 计划解决方向：
  1. 将供应商行从 `36px` 收紧到 `24px`，保留现有 `18px` 状态标记、字号和可用性条尺寸，并重新校准垂直居中，使行内最高元素上下各保留约 `3px`，同时避免文字、标记或分隔线拥挤。
  2. 同步把原生 `TRAY_PROVIDER_MINI_ROW_HEIGHT` 与前端十行滚动容器上限调整为 `24px/行` 和 `240px`；十行时窗口总高度应由当前 `404px` 降至 `284px`，保证 WebView 内容与原生窗口没有底部空白或裁切。
  3. 保持最多显示十行、超过十行后内部滚动、每次新快照滚动回顶部和面板内悬停不关闭等既有行为。
  4. 补充前端密度契约与原生窗口几何测试；Rust/native 验证继续由 GitHub Actions 执行，并在 macOS 普通与高分屏缩放下做实机视觉检查。
- 验收标准：
  - 1、5、10 个供应商时，每行高度均为 `24px`，上下留白较当前 `36px` 版本明显减少，面板随实际行数同步变矮且底部没有额外空白。
  - 供应商名称、不可用原因标记、18 单元状态条和成功/失败计数保持清晰、垂直居中，不重叠、不裁切，行分隔线仍能稳定区分相邻供应商。
  - 超过 10 个供应商时面板高度不继续增长，滚轮可以访问最后一行；滚动、从 Tray 图标移入面板和移出关闭均不出现闪烁或意外关闭。
  - 标题栏、空状态、面板宽度和现有 CLI/路由选择及快照冻结行为无可见或功能回归。
- 实施边界：本条只做 mini 面板密度微调，不改变供应商筛选、排序、路由资格、可用性聚合、数据刷新或 Tray 左右键行为。
