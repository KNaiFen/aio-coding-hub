# Workspace Records

任务 session、writer、claim receipt 和 runtime attachment 由 canonical GKD bundle 管理，不在仓库维护第二套 workspace 状态。

本目录仅保留既有会话记录，不能作为当前任务、路由、验收或恢复依据。当前任务事实请读取 `tasks/` 与 `.gkd/`，并通过 `$gkd-main`、`$gkd-execute`、`gkd_acceptor` 操作。
