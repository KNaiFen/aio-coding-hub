# 设计

父任务只负责需求映射、跨批次契约、集成验收和发布，不直接拥有产品实现。子任务按路由门禁、余额运行时、可用性事实源、TUI、桌面和 macOS Tray 分层；共享协议与设置采用向后兼容的可选字段，SQLite 仅做加法迁移。

功能 PR 保留批次提交历史。功能合并后，机械发布 PR 更新 PENDING 证据和六个版本文件；main 的不可变 SHA 生成候选后再创建 0.60.43 标签并晋升制品。
