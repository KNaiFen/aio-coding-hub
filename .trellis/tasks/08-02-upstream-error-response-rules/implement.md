# 执行计划

- [x] 增加 AppSettings v57 规则类型、容错读取、严格写入校验、字段所有权和
      前端设置映射；由 CI 同步生成绑定。
- [x] 在运行时设置快照中加入规则，并实现有界、纯观察的匹配器、消息提取器和
      三类客户端协议错误响应构造器。
- [x] 将候选证据接入非成功响应与终态处理，保证重试、切换、熔断和健康状态
      先于改写且只使用原始事实。
- [x] 将客户端最终状态和固定审计标记写入主日志，同时保留尝试链原始状态。
- [x] 在 CLI 管理“熔断与重试”下增加规则列表及编辑弹窗，接入供应商列表和
      普通设置保存，完成前后端一致校验。
- [x] 为首页、活动请求和日志页增加 fail-open 规则徽标与提示。
- [x] 添加 Rust 单元/路由测试和前端组件/解析测试；更新后端与跨层规范。
- [x] 本地执行前端测试、typecheck、lint、build，不运行任何 Rust/Tauri 命令。
- [ ] 完成代码审查、安全检查、原子提交并推送 PR；修复 CI 后合入 `main`。
