# 集成设计

- 两项功能共享分支和 PR，但代码与提交边界独立。
- 模型路由会新增跨层配置、供应商列、审计标记和 Observer 可选字段；通知修复复用既有活跃请求 IPC，不新增后端命令。
- 模型路由失败保留当前供应商的未改写出站请求；通知校验失败只跳过提醒。两者都不得改变供应商健康或网关生命周期。
- 数据库变更只新增 nullable 列，旧设置和旧分享文件继续可读；Observer v1 只增加可选字段，不提升协议版本。
