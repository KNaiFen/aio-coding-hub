# 执行计划

- [x] 更新跨层路由与观察者规范，定义有效切换、有效重试和限额感知首选语义。
- [x] 修改首页路由摘要，仅在非 skipped hop 之间统计供应商切换并更新测试。
- [x] 为供应商限额读模型增加安全的“已达上限”判定及边界测试。
- [x] 扩展观察者最小供应商身份投影，加入认证模式并生成只读 limited provider 集合。
- [x] 修改 TUI 快照首选选择与终态路由计数，补充 Rust 单元测试。
- [x] 执行前端目标测试、typecheck、lint、build 和差异检查；记录未运行的原生验证。
- [x] 进行五轴代码审查，确认观察失败不影响转发并完成第三个提交。

## 验证记录

- 目标 Vitest：2 个文件、47 个测试通过。
- 全量前端 Vitest：300 个文件、2625 个测试通过。
- `pnpm typecheck`、`pnpm lint`、`pnpm build`、Prettier 检查与
  `git diff --check` 通过。
- 根据仓库规则，未在本机运行 Cargo、rustfmt、Clippy、Rust 测试或
  Tauri/Specta 生成；这些项目由 GitHub Actions 验证。

## 回滚点

- 前端有效切换计数、限额读模型与观察者选择分别保持独立函数边界。
- 不修改协议或迁移；第三个提交可整体回退，不影响前两个 UI 提交。
