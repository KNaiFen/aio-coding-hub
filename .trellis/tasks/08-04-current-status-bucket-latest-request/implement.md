# 实施清单

1. [x] 激活子任务并创建独立功能分支/worktree。
2. [x] 扩展 timeline 查询为 `rowid` 稳定排序。
3. [x] 在聚合器中记录每个 provider 当前对齐时间段的最后结果，仅覆盖最后显示格状态。
4. [x] 增加当前格、同毫秒和跨时间格的 Rust 单元测试。
5. [x] 静态复核 AIO/TUI/托盘仍只消费共享状态，协议与计数无变化。
6. [x] 本地未运行 Rust 工具；`tsc -p tsconfig.json --pretty false` 与 `git diff --check` 通过，Rust 测试交给 GitHub Actions。
7. [x] 已同步 `origin/main@02b9980d`（PR #51 合并提交）并审计相关主线变化；无直接路径冲突，共享状态消费者和排序语义保持兼容。

## 回滚点

- 数据库无迁移；实现提交可独立回滚。
- 若共享消费者出现非预期差异，保留计数聚合并仅撤销最后格状态覆盖。
