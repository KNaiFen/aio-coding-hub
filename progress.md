# 进度

## 已完成

- 从最终上游 attempt 计时状态暴露未过滤的观测时长，用于精确字段无效时的候选估算。
- 将候选时长贯通请求结束、日志入队、SQLite 迁移/查询、观察协议和生成 bindings。
- TUI 与桌面请求日志统一优先精确值，再显示 `≈` 估算值；聚合仍只读取精确字段。
- 移除旧的基于整请求 `duration_ms`、单次尝试限制的估算条件。
- 更新 TUI、桌面日志夹具，覆盖精确优先、同口径估算、失败和无输出场景。

## 验证

- 已运行：`git diff --check`，通过。
- 未运行：`scripts/gkd-verify --base-sha <full-lowercase-sha>`；当前 worktree 和主 checkout 均不存在该脚本。
- 未运行：Rust 测试、lint、类型检查、构建和开发服务器；仓库规则明确禁止本地运行。

## 剩余风险

- 未在完整 TUI/桌面构建环境中执行编译和测试，需由云端 CI 验证字段生成和跨 crate 类型一致性。
- 候选值仅在完成边界可观测且未记录错误/污染时写入；不对历史日志回填。
