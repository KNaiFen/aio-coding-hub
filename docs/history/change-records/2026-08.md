# 2026-08 简单变更记录

> **资料类型：历史变更证据。证据月份：2026-08。** 本文件记录 main session 直接实施的简单任务，以及任务方案/结果留痕规则建立时的收口证据。当前 GKD 入口见 [GKD 项目 adapter](../../operations/gkd-adapter.md)。

## 2026-08-10 — 完善 main Plan 模式与任务留痕规则

- 状态：完成
- 负责人：main session
- PR：[KNaiFen/aio-coding-hub#105](https://github.com/KNaiFen/aio-coding-hub/pull/105)

### 确认方案

#### 问题与目标

- 将未跟踪的 `upgrade-tui.command` 加入待排查列表，本轮不读取、运行或处置。
- 要求 main 在 Plan 模式主动细化需求、定位问题、明确目标；用户需求和产品决定不确定时必须询问，不能猜测。
- 明确简单任务可以由 main session 在 `main/` 检出直接施工，不强制创建 worktree。
- 明确所有已经确认并进入实施的方案，以及成功、失败或部分完成的最终结果，都必须有仓库内 Markdown 记录。

#### 范围

- 修改 `PENDING.md` 和仓库内 `AGENTS.md`；同步更新仓库外的父目录协调 `AGENTS.md`，使本地 main/执行 session 分工采用同一口径。
- 新增通用任务留痕规范、简单任务模板和月度记录。
- 调整多 worktree 规范及知识库索引，使简单任务与委派任务采用不同施工路径。
- 不调查或修改 `upgrade-tui.command`，不改变产品代码。

#### 用户锁定决定

- 不把 worktree 设为所有任务的强制要求。
- 不新增 JSON 硬门禁。
- 用户意图不确定时必须询问；可从仓库确定的技术事实由 main 主动调查。
- 方案和最终结果都必须落入 Markdown。

#### 实现方向

- 在仓库内 `AGENTS.md`、`PENDING.md`、Trellis 工作流和任务索引中统一两条路由：简单任务使用月度变更记录，复杂或委派任务使用 Trellis 与独立 session 交付文档。
- 复用现有 `execution.md`、`delivery.md` 和 `findings.md` 模板，不新增 JSON 状态机或审批表。
- 由知识库和历史索引链接规范与月度记录，使后续 main session 能从单一入口找到现行规则和历史结果。

#### 验收与验证

- `PENDING.md` 使用下一个稳定 ID 登记脚本排查事项。
- main `AGENTS.md` 明确 Plan 模式、任务分流和 Markdown 留痕要求。
- 规范能分别指导简单 main 任务和独立 worktree 任务。
- 相对链接、云端验证边界合同、文档合同和 `ci-gate` 通过。

#### 风险与停止条件

- 若发现来源不明的未跟踪文件、用户工作或与其他活动任务重叠的修改，停止纳入或处置并向用户确认；本轮对 `upgrade-tui.command` 仅登记，不读取或执行。
- 若简单任务在实施中扩大为并行、长流程、跨模块或高风险工作，保留已有记录并迁移到独立 worktree。
- 本地不得运行依赖安装、构建、测试或格式化；完整质量门由 PR 的 GitHub Actions 执行。

### 实施结果

- 结果：完成。仓库内和父目录协调规则均已明确简单任务可由 main session 直接实施，复杂或委派任务才使用独立 worktree；确认方案和实际结果分别有对应 Markdown 记录路径。
- 代码与文档位置：`AGENTS.md` 规定 Plan 模式、任务路由和 PENDING 关联方式；`PENDING.md` 登记 `AIO-PENDING-029`；`.trellis/workflow.md`、`.trellis/tasks/README.md` 和 `.trellis/scripts/common/task_store.py` 明确简单任务可跳过 Trellis，并让 CLI 提示区分 main、委派和复杂任务；`docs/operations/task-documentation-records.md`、`docs/operations/multi-worktree-delivery.md` 和模板定义两类任务的记录、交付、验收和返工内容；`docs/README.md` 与 `docs/history/README.md` 提供入口。仓库外父目录 `AGENTS.md` 同步了 main/执行 session 的本地协调口径。
- 验收结果：AC-01 通过，`AIO-PENDING-029` 已登记且下一 ID 为 `AIO-PENDING-030`，脚本未被读取或处置；AC-02 通过，main 的 Plan 模式职责和不得猜测的边界已写入仓库规则；AC-03 通过，简单 main、轻量委派和复杂委派三种文档要求已统一到 AGENTS、Trellis breadcrumb、启动门槛与交付规范；AC-04 通过，本地文档/边界检查与 PR 首轮 `docs-contract`、`ci-gate` 均为绿色。
- 方案偏移：本次规则调整沿用用户澄清前已经创建的 `plan-mode-rules` worktree；新路由适用于后续任务，不为迁移本次施工而制造额外历史。独立验收发现 Trellis breadcrumb 和 CLI 生成提示仍可能绕过月度记录或漏掉 `execution.md`，已在合并前补齐。父目录协调 `AGENTS.md` 不在 Git 仓库中，因此不属于 PR diff，但已在本地同步并在 PR 范围说明中披露。
- 验证：`git diff --check`、`node scripts/check-markdown-links.mjs`、`node scripts/check-cloud-only-verification.mjs` 均通过；PR #105 首轮 head `b3c4a02a` 的 [GitHub Actions run 31335290511](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31335290511) 中 `docs-contract` 与 `ci-gate` 通过，其余构建按纯文档范围跳过。本记录提交后仍须以 PR 最新 head 的实时绿灯为合并依据。
- 交付证据：[PR #105](https://github.com/KNaiFen/aio-coding-hub/pull/105)；实施提交 `b3c4a02a`。
- 文档与合同：已更新仓库执行规则、Trellis 工作流/任务索引、任务留痕规范、多 worktree 交付规范、简单任务模板、知识库索引和本月历史记录；未改变产品代码或运行时机器合同。
- 遗留事项：`AIO-PENDING-029` 保持待排查，未经用户后续授权不调查或处置 `upgrade-tui.command`。

## 2026-08-10 — 补强多 Worktree 交接与验收闭环

- 状态：完成
- 负责人：main session
- PR：[KNaiFen/aio-coding-hub#106](https://github.com/KNaiFen/aio-coding-hub/pull/106)
- 实施授权：已确认（2026-08-10；用户要求按审查建议补强当前流程）
- 方案版本：2026-08-10 本月条目
- 执行路线：直接 main

### 确认方案

#### 问题与目标

- 现有流程已经覆盖需求、计划、执行、交付、返工和知识库维护，但“用户确认的是哪一版方案”“当前唯一写者是谁”“交付、CI 和验收是否针对同一提交”“合并后如何留下最终结论”等交接事实尚未完全固定。
- 在不引入自定义 JSON 门禁和额外审批系统的前提下，补强现有 Markdown 规范与模板，使 main、执行 session 和验收者可以只依赖仓库记录完成可追溯交接。

#### 范围

- 必须完成：补强方案确认、活动任务索引、worktree 启动基线、暂停与写权转移、交付候选、main 验收、返工复验、合并收尾、归档和知识库回写规则。
- 允许修改：协调目录与仓库内 `AGENTS.md`、`docs/operations/` 规范和模板、`.trellis/workflow.md`、`.trellis/tasks/README.md`、`.trellis/scripts/task.py` 与任务创建元数据提示、本月变更记录，以及为保持这些规则一致所必需的无产品运行时影响文档合同。
- 不在范围内：产品代码、Trellis JSON 状态机、GitHub 分支保护设置，以及 `upgrade-tui.command` 的调查或处置。

#### 用户锁定决定

- 继续复用 `prd.md`、`design.md`、`implement.md`、`execution.md`、`delivery.md` 和 `findings.md`；不增加 `acceptance.json`、审批表或其他自定义 JSON 门禁。
- 简单、低风险且由 main 连续完成的任务仍可直接在 `main/` 检出施工；只有委派、并行、长流程或高风险任务才需要同级 worktree。
- 执行 session 负责把 PR 最新提交的必需 CI 和相关编译跑绿，main 负责针对该提交验收并决定是否合并。
- `AIO-PENDING-029` 继续延期，本次不读取、执行、修改、移动或删除 `upgrade-tui.command`。

#### 事实、假设与未决问题

| 项目 | 来源 | 状态或关闭条件 |
|---|---|---|
| 现有规范、模板与 Trellis CLI 已覆盖基本流程，但缺少版本、写权和收尾事实。 | 本轮五个只读流程审查与当前仓库文档 | 已确认；本次以现有 Markdown 补强。 |
| 不需要新的 JSON 状态机或审批文件。 | 用户锁定决定与现有规范 | 已确认。 |
| 是否需要修改产品代码、GitHub ruleset 或调查未跟踪脚本。 | 本次范围与 PENDING | 不在范围内；不作为本轮条件。 |

#### 实现方向

- 在现有计划和交付文件中增加少量稳定字段，不创建新的独立审批文件：计划记录确认状态与变更历史，`execution.md` 固定基线和写权交接，`delivery.md` 同时承载交付候选、main 验收和最终收尾，`findings.md` 绑定每轮复验版本。
- 将 `.trellis/tasks/README.md` 明确为 main 的活动任务协调索引，只记录定位和状态，不复制详细方案。
- 修正 Trellis 文档中不存在的命令、归档默认方式和归档后失效的模板引用；让任务创建默认指向受保护的 main，并提供显式的其他 base 分支参数，使文档规则与 CLI 行为一致。

#### 验收与验证

- AC-01：确认方案能够区分草案、实施授权、材料性未决问题和后续范围变更，执行 session 不必从聊天记录猜测。
- AC-02：委派任务能够从 main 索引和 `execution.md` 确认 base SHA、规划提交、分支、worktree、PR、当前阶段和唯一写者。
- AC-03：每次交付、CI、main 验收和返工复验均绑定同一完整 PR head SHA；新提交会明确使旧结论失效。
- AC-04：完成、部分完成、失败、阻塞和放弃均有 Markdown 收尾位置；合并、知识库、PENDING、归档和 worktree 清理有确定顺序与证据。
- AC-05：现行规范、模板、Trellis 工作流和任务索引不存在互相冲突的命令、链接或职责描述；本地允许的文档合同、链接检查和 `git diff --check` 通过。

#### 风险与停止条件

- 若补强必须依赖新的 JSON 状态机、外部审批系统或改变产品运行时行为，停止扩大范围并交用户重新决定。
- 若发现其他来源不明的修改或活动任务冲突，不纳入本次提交；`upgrade-tui.command` 始终保持未触碰。
- 本地不得运行依赖安装、构建、测试或格式化；完整质量门由 PR 的 GitHub Actions 执行。

#### PENDING 审阅

- AIO-PENDING-029 延后；用户禁止调查，且与本轮流程治理无依赖。

#### 范围与决定变更

| 日期 | 原/新范围或决定 | 影响的 AC/验证 | 决定人和恢复条件 |
|---|---|---|---|
| 无 |  |  |  |

### 实施结果

- 结果：完成。流程、模板、Trellis CLI 提示和回归测试已通过 main 验收；功能交付已由 PR #106 合并到 `main`，本条收尾记录负责持久化最终证据。
- 代码与文档位置：父目录协调 `AGENTS.md` 与仓库 `AGENTS.md` 固定 main/执行 session 的路由、写权和收尾边界；`docs/operations/task-documentation-records.md`、`multi-worktree-delivery.md` 及四个模板定义确认、迁移、交付、验收、返工和无功能 PR 终态；`.trellis/workflow.md`、`.trellis/tasks/README.md`、`.trellis/scripts/task.py` 与 `common/task_store.py` 对齐实际命令、默认 PR base、独立执行例外和归档语义；`.trellis/scripts/tests/test_task_routing.py` 覆盖新任务 base、无 session identity 的降级启动、状态读取/写入失败及指针清理；Claude OAuth 任务元数据只清除了已删除的旧分支定位并保留暂停/重新确认要求。
- 验收结果：AC-01 通过，计划记录和默认 PRD 能区分授权、版本、事实/假设/未决问题与后续决定变更；AC-02 通过，活动索引与 `execution.md` 固定 base SHA、规划提交、分支、worktree、PR、阶段、唯一写者和精确 merge-base 检查；AC-03 通过，`delivery.md`、`findings.md` 和 main 验收轮次统一绑定完整 PR head 与 `ci-gate`；AC-04 通过，含功能 PR和无功能 PR 的完成、部分完成、失败、阻塞、放弃均有收尾、归档或保持活动路径；AC-05 通过，PR #106 的冻结 head `8c28ac8e91dc9c146f788d6e2a31bccf8f6ac726` 在 `ci-gate`、`frontend`、`rust`、`docs-contract`、`support-contract`、`change-scope` 和 `pr-title` 全部绿色时经 main 验收，候选构建 job 按范围跳过。
- 方案偏移：终审发现通用 Trellis Phase 3 会误导独立执行 session 提前 `/trellis:finish-work`/归档，且无功能 PR 的终态与简单任务迁移缺少闭环；已在原范围内补上显式执行例外、只含记录的收尾 PR、可判定基线命令和迁移反向链。为 CLI 的默认 base 与降级启动新增标准库单测；未增加 JSON 状态机或审批表。另按现状清理 Claude OAuth `task.json` 的失效分支字段，不调查或实现该候选。
- 验证：`git diff --check`、`node scripts/check-markdown-links.mjs`、`node scripts/check-cloud-only-verification.mjs`、`node scripts/check-cloud-only-verification.selftest.mjs`、`node scripts/check-plugin-system-docs.mjs`、`node scripts/check-plugin-api-contract.mjs` 均通过。按零产物规则未在本地运行 Python 单测、依赖安装、构建、格式化或产品测试；[GitHub Actions run 31362589614](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31362589614) 对上述冻结 head 运行完整质量链，`ci-gate`、`frontend`、`rust`、文档与支持合同均通过。
- 交付证据：[PR #106](https://github.com/KNaiFen/aio-coding-hub/pull/106)，基线 `e2182ca301737b6c67ba540aded6eca490a3c5a7`，验收候选 `8c28ac8e91dc9c146f788d6e2a31bccf8f6ac726`，实际 merge commit `9b7de41fbc592e347f78f1b91df710027243c1a0`。
- 文档与合同：已更新两套 `AGENTS.md`、知识库入口、任务留痕/多 worktree 现行规范、施工/交付/整改/简单记录模板、Trellis 工作流/任务索引/默认 PRD 和 CLI 帮助；Trellis 创建行为现在默认 `base_branch=main` 并可显式覆盖。未改变产品运行时、公共 API 或插件机器合同。
- 遗留事项：`AIO-PENDING-029` 继续延期，`upgrade-tui.command` 未进入 diff、提交或验证范围；Claude OAuth 候选继续暂停，恢复时重新确认方案并设置新分支。
- 收尾：PR #106 已 squash 合并；main 已 fetch/快进到 merge commit `9b7de41fbc592e347f78f1b91df710027243c1a0`。远端 `docs/workflow-hardening` 已删除；本地分支因 squash 不能由安全删除判定祖先关系，但最终树与 `main` 无差异后已删除。知识库更新随功能 PR 合入，PENDING 去向不变；本收尾记录通过短期记录 PR 进入 `main` 后即可清理该分支。
- 过程说明：首次创建 PR 描述时，shell 错误解释 Markdown 反引号；Bash 尝试解析裸名称 `upgrade-tui.command` 后立即报 command not found，未发生该脚本的实际执行、读取或改动。PR 描述已用字面量引用重写；该事件不改变本轮范围或 PENDING 决定。

## 2026-08-12 — 补强新 Worktree 的执行 Session 启动交接

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-12；用户要求 main 在新建 worktree 后主动给出执行 session 的路径、可直接使用的 Prompt 与开工条件）
- 执行路线：`main/` 短期文档分支 `docs/worktree-session-handoff`；main 专属规则写入仓库内 `main/AGENTS.md`，协调根目录 `AGENTS.md` 只保留共享协作边界及指针。

### 确认方案

#### 问题与目标

- 用户不能仅凭活动任务索引或聊天上下文可靠判断应该在哪个目录新开执行 session，容易错误恢复旧 session，或将任务交给不存在、已清理或不属于该 PR 的 worktree。
- main 每次为独立执行任务创建并登记同级 worktree 后，都应主动提供一个完整、可操作的启动交接包，使用户能够在正确目录创建唯一执行 session，并知道何时可以真正开工。

#### 范围

- 修改 `main/AGENTS.md`：新增 main 创建 worktree 后向用户主动交付启动信息的强制规则、最小字段和等待/恢复边界；协调根目录 `AGENTS.md` 仅保留对该 main 专属规则的指针。
- 在本月简单变更记录中保存确认方案和实际实施结果。
- 不修改产品代码、Trellis 任务状态机、GitHub ruleset、PR #114 的任务材料或 CI 行为；不读取、修改或提交 `upgrade-tui.command` 与 `SESSION_REMEDIATION_PLAN.md`。

#### 用户锁定决定

- main 必须主动提供执行 session 的绝对工作路径和可直接粘贴的 Prompt，不能让用户猜测该去哪个 session 或从旧暂停 session 恢复。
- 启动包必须说明任务、分支、基线/规划材料、当前唯一写者、允许开工的条件、禁止事项、交付/暂停标准以及 main 的下一道验收门。
- 当 PR 最新 head 的必需 CI 仍在运行、失败、未知或候选发生漂移时，main 必须明确说明“暂不启动”，持续监控，并仅在对应候选绿色后通知用户在新 session 开工。

#### 事实、假设与未决问题

| 项目 | 来源 | 状态或关闭条件 |
|---|---|---|
| 协调根目录 `AGENTS.md` 已定义共享的 main、执行 session、worktree、写权、CI 与交付边界；仓库内 `main/AGENTS.md` 是 main 专属启动交接规则的持久化位置。 | 当前规则与用户反馈 | 已确认；本次补充。 |
| 协调根目录不在 Git 仓库中；根目录的精简指针无法作为本次文档分支的 Git diff，main 专属正文和本月记录则在 `main/` 中持久化。 | `git rev-parse --show-toplevel` 对该目录返回非仓库 | 已确认；分层记录。 |
| PR #114 的缺失自动检查表明：main 不能只把“没有 check”解释为排队，必须先区分候选是否因合并冲突而不能形成普通 PR CI。 | 本轮 GitHub 状态与本地三方合并核验 | 已确认；详细证据由下列 main 协调记录保存，执行材料只保留可操作的冲突恢复条件。 |

#### Main 协调记录：PR #114 的 CI 触发与冲突恢复

- 授权提交 `e4e797e42274e8c87f70121d70f9c51a160be9f5` 和后续记录更新 `1326a6f391a15d6c351bbc367bc232c15087d88e` 都没有 Actions run、check suite 或 check run；同仓库 Draft PR #119 的 CI 正常启动，且 `ci.yml` 的 `pull_request` 没有路径或分支过滤。
- PR #114 当前 head `cfb6d32cd4dd2f3a170cd14e229400600b8192ac` 为 `DIRTY`/`CONFLICTING`；`git merge-tree --name-only --messages origin/main HEAD` 只报告 `.trellis/tasks/README.md` 内容冲突。
- 用户授权修复触发缺失后，main 的决定是：不改 CI YAML、不手动 dispatch、不推空提交；先把唯一 README 冲突交接给阶段 B 的唯一执行 session。执行 session 推送干净新 head 后，main 再针对该 head 监控自动 CI。
- 该诊断、候选历史和用户通知属于 main 协调事实；#114 的任务材料仅保留“唯一 README 冲突必须先解决，之后等待新 head CI”这一施工条件。

#### 实现方向

- 在 `main/AGENTS.md` 中定义“执行 session 启动包”，采用固定信息清单而不是复制任务正文；协调根目录只指向 main 专属规则，详细方案仍只以任务 worktree 中的 `execution.md`、`prd.md` 等为准。
- 要求 main 以绝对路径、分支、PR/head、当前写者和启动状态消除会话定位歧义；对旧/已清理目录明确要求关闭而非复用。
- 将 CI 结果与可开工通知绑定到同一完整 head SHA，防止用户在过期或未验证的候选上启动执行 session。
- 将 main 专属的监控、诊断、用户沟通和跨 worktree 决定留在 main 记录；任务材料只保留执行者必须据以行动的简短条件，防止执行 session 从冗长协调历史中误推新的范围。

#### 验收与验证

- `main/AGENTS.md` 明确 main 在每次新建执行 worktree 后必须主动向用户发送启动交接包；协调根目录不得复制该 main 专属正文。
- 交接包的必填字段至少覆盖任务/目标、绝对路径、分支、base SHA/规划材料、PR/head/CI、唯一写者、可开工状态、可粘贴 Prompt、范围边界、交付标准和 main 后续门。
- 规则明确：CI 未终态绿色、head 漂移、worktree 缺失或来源不明改动时不得通知开工；旧暂停 session 不得被复用。
- main 专属协调事实不再复制到执行 session 材料；执行材料仍能独立说明当前可做的动作、停止条件和交付门。
- 月度记录同时包含确认方案与实际结果；Git diff 仅包含本次 main 专属规则及其留痕，`git diff --check` 通过。

#### 风险与停止条件

- 若新增内容迫使修改产品行为、GitHub 保护规则、Trellis JSON 门禁或活动任务材料，停止扩大范围并重新确认。
- 根目录外部规则与仓库内记录必须保持语义一致；如发现同名 session、路径或 head 信息无法确认，main 先报告而不发出可开工通知。

#### 范围与决定变更

| 日期 | 原 / 新范围或决定 | 影响的验收与验证 | 决定人和恢复条件 |
|---|---|---|---|
| 2026-08-12 | 原范围只建立启动交接包。用户进一步确认：与执行 session 无关的协调内容必须留在 main；因此允许 main 最小化清理 PR #114 任务材料中的 CI 轮询/诊断时间线，同时保留执行所需的 README 冲突恢复条件。 | 启动交接信息归属、#114 施工入口与 main 记录一致性 | 用户；不改变产品代码、工作流、任务范围或交付责任。 |
| 2026-08-12 | 用户进一步澄清：根目录 `AGENTS.md` 不承载冗长的 main 专属流程；启动交接、CI 启动门、监控和信息归属规则迁入 `main/AGENTS.md`，根目录仅保留共享协作规则及指针。 | 规则读取路径、main 交接行为与根目录复杂度 | 用户；新文件必须被 `main/` session 自动读取，根目录不得复制同一规则正文。 |
| 2026-08-12 | 用户再次澄清：执行 session 没有固定文件夹，因此执行 session 的完整项目级操作、交付、停止和验证规则必须留在协调根目录；只有真正仅 main 使用的计划、worktree 登记、启动交接、实时监控、验收、合并、归档和清理流程独立放入 `main/AGENTS.md`。 | 执行 session 在任意 sibling worktree 的规则可见性；main-only 内容不再污染共享入口 | 用户；根目录保留执行所需边界，main 文件只补协调者职责。 |

### 实施结果

- 结果：完成。main 专属启动交接、CI 启动门、旧 session 处置与信息归属规则已写入 `main/AGENTS.md`；协调根目录已精简为共享协作边界和指针，不再承载该冗长正文。
- 启动门：规则区分“纯规划登记不自动阻塞施工”和“用户、任务计划或候选明确要求先等 CI”的情形。后者必须将通知绑定到同一完整 head，且只在必需检查绿色、head 未漂移、worktree 存在且状态可归属后，明确通知用户在给定绝对路径新开 session；其余状态一律标为“暂不启动”。
- 会话恢复：已补入旧暂停 session 的核验规则。已删除、归档、合并或不属于当前 PR 的目录必须关闭且不得复用，避免用户把新任务交给历史 session。
- 实际范围：已修改仓库内 `main/AGENTS.md`、仓库外协调 `AGENTS.md` 的简短指针与本月历史记录；本次范围变更允许最小化清理 PR #114 任务材料中的 main 专属协调时间线，但不修改产品代码、Trellis 状态机、GitHub ruleset 或 CI 行为，且不读取、修改或提交 `upgrade-tui.command` 与 `SESSION_REMEDIATION_PLAN.md`。
- 验证：`git diff --check`、Markdown 链接检查和对应文档 PR 的最新 CI 用于验证仓库内修改；根目录精简指针不在 Git 仓库中，无法纳入 PR diff，但其语义与 `main/AGENTS.md` 的规则归属一致。

## 2026-08-12 — 按 Session 职责整理 AGENTS.md

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-12；用户要求协调根目录不要承载过多仅供 main 阅读的规则，main 专属准则统一放入 `main/AGENTS.md`）
- 方案版本：2026-08-12 本月条目
- 执行路线：直接 main，短期文档分支 `docs/scope-agents-by-session`

### 确认方案

#### 问题与目标

- 协调根目录 `AGENTS.md` 会被 main 与任意 sibling task worktree 中启动的 session 作为工作区级入口读取；执行 session 没有固定文件夹，不能依赖某个任务目录补齐项目级操作规则。
- 按职责重新分层：协调根目录保留所有 session 共用的项目合同、执行 session 的完整施工/交付/停止规则和 Git 安全边界；真正只由 main 执行的规划、建树登记、启动交接、实时监控、验收、合并、归档、清理、知识库和 PENDING 收尾规则放入 `main/AGENTS.md`。

#### 范围与锁定决定

- 整理仓库外协调文件 `/Users/knaifen/Documents/Codex/aio-coding-hub/AGENTS.md`，把执行 session 在任意 worktree 都必须知道的远端、GitHub CLI、零产物、云端验证、交付、暂停和禁止操作写在这里。
- 更新仓库内 `main/AGENTS.md`，只承接 main session 的规划、任务路由、worktree 登记、用户交接、CI/head 协调、验收返工、PR 合并、归档清理、知识库和 PENDING 规则；保留 CI 静态合同要求的短锚点，不复制执行细则。
- 不改变既有权限、流程顺序、Markdown 文件职责、CI 合同或产品行为；不修改 Trellis 任务材料、产品代码、GitHub 配置或 `PENDING.md`。
- `AIO-PENDING-029` 与本次职责分层无关，继续保持 `pending`，不读取、执行或处置其目标文件。

#### 验收与验证

- 根目录文件不再承载 main 的实时协调时间线或验收决策细节，但保留执行 session 开工、施工、交付、暂停和停止所需的完整规则。
- 执行 session 无论落在哪个 sibling worktree，都能从协调根目录明确远端、验证、目录/分支核对、唯一写者、允许/禁止操作、交付证据和暂停责任。
- `main/AGENTS.md` 只描述 main 的任务路由、登记、交接、监控、验收、返工、合并、收尾和知识库/PENDING 处置；仓库合同锚点仅因静态检查保留。
- 两份规则不存在同一 main-only 流程的大段重复；仓库内变更通过 `git diff --check`、Markdown 链接检查、云端验证合同和 Trellis manifest 验证。

#### 风险与停止条件

- 若精简会使执行 session 看不到影响其自身行为的规则，则该规则保留在协调根目录；不以减少行数为目的牺牲开工、写权、范围或交付安全边界。
- 协调根目录不属于 Git 仓库，无法随 PR 持久化；main 合并前必须在本机对照检查它与仓库 `main/AGENTS.md` 的职责分层，并在交付记录中说明这一事实。

### 实施结果

- 结果：完成。协调根目录 `AGENTS.md` 现承载所有 session 共用的项目操作合同和执行 session 完整规则；仓库 `main/AGENTS.md` 现只承载 main 协调 session 的规划、建树登记、启动交接、CI/head 监控、验收返工、合并、归档、清理、知识库和 PENDING 收尾职责，并保留静态合同检查需要的短锚点。
- 关键边界：执行 session 不依赖固定任务文件夹来获得项目级规则；它从协调根目录读取完整施工入口、零产物/云端验证、PR、交付、暂停和禁止操作。main-only 的实时诊断、用户沟通和生命周期决策不再放入共享入口。
- 实际文件：仓库外 `/Users/knaifen/Documents/Codex/aio-coding-hub/AGENTS.md`、仓库内 `main/AGENTS.md` 和本条月度记录；未修改产品代码、Trellis 状态机、GitHub ruleset、`PENDING.md`、`upgrade-tui.command` 或 `SESSION_REMEDIATION_PLAN.md`。
- 验证：`git diff --check`、`node scripts/check-cloud-only-verification.mjs`、`node scripts/check-cloud-only-verification.selftest.mjs`、`node scripts/check-markdown-links.mjs`、`python3 ./.trellis/scripts/task.py validate --all` 和职责分层断言均通过；未运行依赖安装、构建、格式化或产品测试。
- 交付证据：功能 PR [#126](https://github.com/KNaiFen/aio-coding-hub/pull/126)，接受 head `b8c21e90d4aa2fefb0791309ac2240c5ee18f9be`，已由 main squash merge 为 `b9c46b86affcb04df3dde5e93b2a6ed469038e27`；外层协调文件不在 Git 仓库中，最终内容以本机工作区规则和本条记录为准。
- 遗留事项：`AIO-PENDING-029` 继续保持 `pending`，未读取、执行或处置 `upgrade-tui.command`。

### Main 收尾

- PR #126 的 base 为 `fb35c9da23a56e20225fa6d98cc7cd91a943e49b`，接受 head 为 `b8c21e90d4aa2fefb0791309ac2240c5ee18f9be`，GitHub 状态为 `MERGED`，实际 merge commit 为 `b9c46b86affcb04df3dde5e93b2a6ed469038e27`。
- 绑定该 head 的 `ci-gate` 运行 [31605353397](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31605353397)，`pr-title` 运行 [31605353399](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31605353399)，CodeQL 运行 [31605353425](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31605353425)；`ci-gate`、`pr-title`、docs-contract、change-scope、CodeQL JavaScript/TypeScript 与 Rust 均成功，按文档范围跳过的 frontend/rust/support-contract/candidate jobs 未构成阻塞。
- 本地允许验证在功能 PR 合并前已通过：`git diff --check`、`node scripts/check-cloud-only-verification.mjs`、`node scripts/check-cloud-only-verification.selftest.mjs`、`node scripts/check-markdown-links.mjs`、`python3 ./.trellis/scripts/task.py validate --all`（131 manifests）。未运行依赖安装、构建、格式化或产品测试。
- 功能分支 `docs/scope-agents-by-session` 已在 PR 合并后由 GitHub 删除；记录收尾 PR [#127](https://github.com/KNaiFen/aio-coding-hub/pull/127) 的 head `1657bb4421839c6b88eb3636dab9355b25525327` 已 squash merge 为 `95609f505823a4ba55f35f99f5c9126bea746197`，远端收尾分支已删除。当前没有任务 sibling worktree 需要清理，外层协调 `AGENTS.md` 仍作为本机规则保留。

## 2026-08-12 — 验收低风险返工由 main 直接收敛

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-12；用户明确接受“交付/设计记录的小错误由 main 直接修复，涉及长时编译或实质行为的返工仍交回执行 session”）
- 方案版本：2026-08-12 本月条目
- 执行路线：直接 main，短期文档分支 `docs/main-direct-acceptance-fix`

### 确认方案

#### 问题与目标

- 现行规则把所有验收不通过都默认交回执行 session，导致交付文档漏字段、过期 SHA/链接、设计文档事实性小错误等低风险问题也产生一次完整返工等待。
- 增加一个受控的 `main-direct-fix` 例外：main 在不改变任务语义和不触发长时编译/构建流程的前提下，直接在原任务 worktree、分支和 PR 上修复并重新验收。

#### 范围与锁定决定

- 允许 main 直接修复任务记录、`delivery.md`、`findings.md`、设计/施工文档中的缺失字段、状态/路径/SHA/链接/日期错误、拼写格式和对已锁定决定的事实性澄清。
- 设计文档只有在修正事实或表达、且不改变用户决定、范围、AC、架构、接口、数据流、兼容性或安全边界时才属于例外。
- 涉及产品代码、测试逻辑、workflow/policy/selftest、生成文件、依赖、公共 API、迁移、架构或会触发 `frontend_ci`、`rust_ci`、`full_ci`、构建/生成/打包/性能任务的修改，必须交回执行 session。
- main 直接修复前必须确认执行 session 已暂停并接管临时唯一写权；固定在原任务 worktree、原任务分支和原 PR 上，不创建平行修复 lineage。
- 任何新 head 都使旧交付和验收结论失效；修复后必须重新查询该 head 的 CI 并记录证据。若实际 scope 意外选中长任务，main 立即停止该例外路径并把后续处理交回执行 session。

#### 验收与验证

- `findings.md` 保留原始问题，并标明 `main-direct-fix`、责任接管原因、修改边界和复验标准；无需等待执行 session 填写回应。
- `delivery.md` 记录 main 的直接修改、完整新 head、对应 `ci-gate` 和新的验收轮次。
- 仅执行项目允许的依赖无关本地检查；文档/记录变更应由自动短 CI 验证。CodeQL 的 `build-mode: none` 扫描本身不等同于长编译，但仍须等待相关检查终态后才能验收。

#### 风险与停止条件

- “改动行数少”或“文件后缀是 Markdown”不是充分条件；只要语义、范围、风险或 CI 成本不明确，就退回执行 session并暂停。
- 本次只更新 main 专属规则、交付规范和模板，不修改产品代码、工作流、Trellis 状态机、活动任务材料或 `PENDING.md`。

### 实施结果

- 结果：完成。main 的验收返工规则现提供受控的 `main-direct-fix` 例外：交付/验收记录缺字段、过期 SHA/链接、路径/状态/日期/拼写/格式错误，以及不改变决定的设计文档事实性小修正，可由 main 在原任务 worktree、分支和 PR 上直接收敛；产品代码、测试逻辑、workflow/policy/selftest、依赖/锁文件、生成文件、公共 API、迁移、架构、shared/未知路径或长时编译/构建/生成/打包/监控任务必须交回执行 session。
- 关键控制：main 接管前必须落盘唯一写者、冻结 head、工作树和未提交内容归属；direct-fix 必须记录完整 `change-scope` 字段与选中/跳过 jobs，实际触发长任务时停止并按记录交回执行 session。新 head 仍使旧交付/验收失效，必须重新等待 CI 并验收。
- 实际文件：`AGENTS.md`、`docs/operations/multi-worktree-delivery.md`、`docs/operations/templates/findings.md`、`docs/operations/templates/delivery.md` 和本条记录；未修改产品代码、workflow、Trellis 状态机、活动任务材料或 `PENDING.md`。
- 验证：`git diff --check`、`node scripts/check-cloud-only-verification.mjs`、`node scripts/check-cloud-only-verification.selftest.mjs`、`node scripts/check-markdown-links.mjs`、`python3 ./.trellis/scripts/task.py validate --all`（131 manifests）均通过；未运行依赖安装、编译、构建或产品测试。首个 PR 候选 `63c9bf9f1be47851f14c3874257e34b2a9508039` 的 `ci-gate` [run 31612922563](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31612922563)、`pr-title` [run 31612922575](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31612922575)、CodeQL [run 31612922542](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31612922542) 均成功；`change-scope`、`docs-contract` 和两项 CodeQL 成功，frontend/rust/support-contract/candidate jobs 按文档范围跳过。后续记录提交会产生新 head，必须重新查询其 CI。
- 收尾：功能文档 PR [#129](https://github.com/KNaiFen/aio-coding-hub/pull/129) 接受 head `cd7c7d310b842590e2391be82316b175380ed7a4`，绑定该 head 的 `ci-gate` [run 31613380959](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31613380959)、`pr-title` [run 31613381006](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31613381006)、CodeQL [run 31613380951](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31613380951) 均成功，实际 squash merge commit 为 `69d710f76dd045d831c8910b6a20e9cdfb012e54`；远端功能分支已删除，main 已快进到该提交。当前没有 sibling worktree 需要清理。

## 2026-08-13 - 合并项目 AGENTS.md 并节流长时 CI 轮询

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-13；用户确认仓库根 `AGENTS.md` 会随 Git worktree 自然检出，要求合并协调根与仓库根规则并删除重复入口，同时在 Codex 全局规则增加长时 CI 轮询间隔）
- 方案版本：2026-08-13 本月条目
- 执行路线：直接 main，短期文档分支 `docs/unify-agent-rules`

### 确认方案

#### 问题与目标

- 仓库外协调根 `/Users/knaifen/Documents/Codex/aio-coding-hub/AGENTS.md` 与仓库内 `AGENTS.md` 当前按层级合并，但仓库内文件本身会随新 worktree 检出；继续保留两份入口增加重复、漂移和上下文成本。
- 将共享项目合同、执行 session 规则和 main 专属协调规则合并进仓库跟踪的根 `AGENTS.md`，删除仓库外协调根文件，并消除重复的角色、Git 安全、交付和 Trellis 段落。
- 在 `/Users/knaifen/.codex/AGENTS.md` 的 CI 子代理例外中增加长时监控节流：常规轮询默认间隔 3-5 分钟，只在终态、失败、目标提交漂移、明确事件或截止时间触发额外检查，避免密集轮询污染上下文。

#### 范围与锁定决定

- 仓库内修改限定为 `AGENTS.md`、`docs/operations/templates/execution.md` 和本条月度记录；仓库外修改限定为删除协调根 `AGENTS.md` 与更新 Codex 全局 `AGENTS.md`。
- 合并只改变规则位置和去重表达，不改变 main/执行 session 权限、唯一写者、零产物、云端验证、任务记录、验收返工、归档清理或 upstream 安全边界。
- 保留云端合同检查要求的精确锚点；历史记录中的旧文件分工保持为当时事实，不回写历史段落。
- `AIO-PENDING-029` 与本次无关，继续保持 `pending`，不读取、执行或处置 `upgrade-tui.command`。

#### 验收与验证

- 新 worktree 只需读取其仓库根 `AGENTS.md`，即可同时获得共享合同、执行 session 施工边界和 main 专属流程；模板不再要求读取已删除的协调父目录文件。
- 仓库外协调根 `AGENTS.md` 删除后，不存在现行文档或脚本依赖该路径；仓库内链接仍正确指向仓库根文件。
- 运行 `git diff --check`、云端验证合同及 selftest、Markdown 链接检查和 Trellis manifest 验证；不安装依赖、不编译、不构建、不运行产品测试。

#### 风险与停止条件

- 若合并遗漏执行 session 必需规则、CI 静态锚点或造成链接失效，停止删除外层文件并先修正规则正文。
- 全局 CI 轮询规则只约束监控频率，不降低失败、漂移、终态或超时的报告要求，也不阻止有明确事件信号时立即复查。

### 实施结果

- 结果：完成。功能文档 PR [#131](https://github.com/KNaiFen/aio-coding-hub/pull/131) 的接受 head 为 `ab3c38f41dec070f8943afe111e357581c6e747e`，已通过 `change-scope`、`docs-contract`、`ci-gate`、`pr-title` 和 CodeQL JavaScript/TypeScript、Rust；frontend、Rust、support-contract、candidate/release jobs 按文档范围跳过。实际 squash merge commit 为 `eacad4d54527d640ae1204b0c34824bc9abe1923`，main 已与 `origin/main` 同步。
- 规则入口：将共享项目合同、执行 session 的施工/交付/停止规则和 main 专属规划、交接、监控、验收、返工、合并、归档、清理规则合并到仓库根 `AGENTS.md`；保留 `main-direct-fix` 的接管、完整 scope 字段和长任务交回控制。
- 本机配置：删除仓库外重复的 `/Users/knaifen/Documents/Codex/aio-coding-hub/AGENTS.md`；更新 `/Users/knaifen/.codex/AGENTS.md`，要求长时 GitHub Actions/部署监控默认每 3-5 分钟轮询，启动时声明间隔和截止时间，终态/失败/head 漂移等明确事件可立即复查。
- 模板与边界：`docs/operations/templates/execution.md` 改为只要求读取当前 worktree 仓库根 `AGENTS.md`；未修改产品代码、workflow、Trellis 状态机、`PENDING.md` 或 `upgrade-tui.command`。
- 验证：`git diff --check`、`node scripts/check-cloud-only-verification.mjs`、`node scripts/check-cloud-only-verification.selftest.mjs`、`node scripts/check-markdown-links.mjs`、`python3 ./.trellis/scripts/task.py validate --all`（131 manifests）均通过；未安装依赖、编译、构建或运行产品测试。
- 收尾事实：功能分支已由 GitHub 删除；本机协调根 `AGENTS.md` 已删除且不属于 Git 仓库，`/Users/knaifen/.codex/AGENTS.md` 的 3-5 分钟轮询规则保留在本机配置中；无 sibling worktree 需要清理。

## 2026-08-13 - 发布 AIO Coding Hub 0.60.51

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-13；用户明确要求发新版本）
- 方案版本：2026-08-13 本月条目
- 执行路线：直接 main，短期版本分支 `chore/release-0.60.51`

### 确认方案

#### 问题与目标

- 当前正式版本为 `0.60.50`，其发布 tag `aio-coding-hub-v0.60.50` 已存在；本次发布将按 fork 规则递增 patch 到 `0.60.51`。
- 发布内容包含 `0.60.50` 以来已合入 main 的功能、CI 治理、可用性恢复和协作规则收尾；版本提交只同步版本源和顶部 CHANGELOG 条目，不修改产品行为。
- 发布必须先通过版本 PR 的自动检查，再等待该精确 main merge SHA 的完整候选 CI，最后创建指向该 SHA 的 annotated tag，由 release workflow 只晋升已验证候选并发布正式 Release。

#### 范围与锁定决定

- 仓库修改限定为 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/crates/aio-observer-protocol/Cargo.toml`、`src-tauri/crates/aio-tui/Cargo.toml`、`src-tauri/Cargo.lock` 中三个 workspace 包版本，以及 `CHANGELOG.md` 和本条记录。
- 所有版本源必须为 `0.60.51`；tag 必须为 `aio-coding-hub-v0.60.51`，且 annotated tag 解引用后的提交必须是 `origin/main` 祖先。
- `AIO-PENDING-029` 继续保持 `pending`，只作为发布前候选清单中的排除项；不读取、执行、修改、移动或删除 `upgrade-tui.command`。
- 不安装依赖，不在本地运行 package-manager、Cargo、Tauri、签名、打包、构建或产品测试；完整验证和候选制品由 GitHub Actions 承担。

#### 验收与验证

- 版本 PR 的最新 head 必须通过 `ci-gate`、`pr-title` 及其按变更范围选中的检查；main 合并后必须记录实际 merge SHA。
- 只接受绑定该 merge SHA 的唯一未过期 `release-candidate-<sha>-<run-id>-<attempt>` artifact；候选 CI 必须包含 Windows x64、macOS ARM64 签名桌面、四平台 TUI 和聚合校验文件。
- annotated tag 推送后，release workflow 必须验证版本一致性、main 祖先关系、12 个资产、`latest.json` 和 `SHA256SUMS.txt`，且不重建或覆盖既有 Release。

#### 风险与停止条件

- 版本源不一致、PR head/merge SHA 漂移、候选缺失/过期/重复、CI 失败、tag 冲突或资产/checksum 不完整时停止发布并记录证据。
- 长时 CI/Release 监控按全局规则默认每 3-5 分钟轮询，启动监控时声明间隔和截止时间；明确终态、失败、head 漂移或其他事件可立即复查。

### 实施结果

- 版本同步：`package.json`、Tauri/Cargo 版本源、`Cargo.lock` 中三个 workspace 包和 `CHANGELOG.md` 已统一为 `0.60.51`；本地依赖无关 release/source/TUI/cloud-only/Markdown/Trellis 合同检查与 `git diff --check` 均通过，未安装依赖或运行产品构建、测试、Cargo、Tauri、签名和打包。
- 版本 PR：[PR #133](https://github.com/KNaiFen/aio-coding-hub/pull/133) 的最终 head 为 `4260e6dccbbd99c518be930429741e85df8ad55a`，自动 `ci-gate`、`pr-title`、frontend、Rust、docs/support contract 和 CodeQL 均通过；实际 squash merge commit 为 `50114c19c2b61c3bec49911489a8795fb532175f`。
- main 候选 CI：[run 31624474459](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31624474459) 始终绑定 merge SHA `50114c19c2b61c3bec49911489a8795fb532175f`。attempt 1、2 的 Windows 桌面编译均成功，但 Tauri 下载 WiX 3.14 时遇到 `protocol: http response missing version`；attempt 3 的 Windows 候选成功，但仅重跑失败 jobs 导致平台产物混用 attempt 2/3，汇总按 attempt 3 精确匹配时失败。随后完整重跑 attempt 4，使 Windows、macOS 和四个平台 TUI 候选使用同一 attempt；frontend、Rust、两项桌面候选、四项 TUI 候选、`assemble-release-candidate` 和 [`ci-gate`](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31624474459/job/94256549044) 全部成功。
- 候选制品：唯一未过期的聚合候选为 `release-candidate-50114c19c2b61c3bec49911489a8795fb532175f-31624474459-4`（artifact `9158090668`）；候选 ZIP 含 12 个发布文件，`SHA256SUMS.txt` 对其余 11 个资产全部校验通过，`latest.json` 为版本 `0.60.51` 并包含 `windows-x86_64`、`darwin-aarch64` 的签名更新信息。
- 正式发布：创建并推送 annotated tag `aio-coding-hub-v0.60.51`，tag 对象为 `948883f087870ddcb0b4504cb53f88283625746e`，解引用后精确指向 merge SHA `50114c19c2b61c3bec49911489a8795fb532175f`。自动 [Release workflow run 31639474612](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31639474612) 成功，只晋升已验证候选；正式 [GitHub Release](https://github.com/KNaiFen/aio-coding-hub/releases/tag/aio-coding-hub-v0.60.51) 已公开，非 draft、非 prerelease。
- 发布后验证：正式 Release 的 12 个资产全部为 `uploaded`；重新下载后，`SHA256SUMS.txt` 对 11 个实际资产全部验证通过，`latest.json` 的版本、签名及两个平台下载 URL 均正确指向 `aio-coding-hub-v0.60.51`。
- 范围外与后续：`AIO-PENDING-029` 继续保持 `pending`；未读取、执行、修改、移动或删除 `upgrade-tui.command`。Homebrew tap 是正式 Release 之后的独立手工步骤，本次未执行，也未将其计入发布完成条件。

## 2026-08-13 - 关闭 AIO-PENDING-029

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-13；用户明确要求将 `AIO-PENDING-029` 标记为已处理）
- 方案版本：2026-08-13 本月条目
- 执行路线：直接 main，短期文档分支 `docs/close-pending-029`

### 确认方案

- 当前 `main` 与 `origin/main` 同步且工作树干净，仓库根 `upgrade-tui.command` 已不存在；只核验路径存在性和 Git 状态，不读取、执行或追溯该脚本内容。
- 将 `AIO-PENDING-029` 从 `PENDING.md` 的活跃列表完整迁入 `PENDING_COMPLETED.md`，状态改为 `done`，保留原问题、锁定决策和验收标准，并记录用户已处理、文件不存在和工作树干净的完成依据。
- 不虚构此前未知的脚本来源、用途或内容安全结论；本次仅关闭已由用户处理、且不再造成工作树脏状态的待办。
- 仅修改 `PENDING.md`、`PENDING_COMPLETED.md` 和本条月度记录；通过依赖无关文档合同、Trellis 验证和短期 records-only PR 交付，不触发本地产品构建或测试。

### 实施结果

- PENDING 迁移已完成：`PENDING.md` 当前无未解决条目，`AIO-PENDING-029` 已完整迁入 `PENDING_COMPLETED.md` 并标记为 `done`；下一个稳定 ID 仍为 `AIO-PENDING-030`。
- 完成事实限定为用户确认已处理、目标文件当前不存在，以及迁移前 main 工作树干净并与 `origin/main` 同步；未读取、执行或追溯 `upgrade-tui.command`，也未补写无法验证的来源、用途或安全结论。
- 本地验证：`git diff --check`、Markdown 链接、cloud-only 合同及 selftest、Trellis 全量验证（131 manifests）均通过；未安装依赖或运行产品构建、测试、Cargo、Tauri、签名和打包。
- 交付证据：records-only PR [#135](https://github.com/KNaiFen/aio-coding-hub/pull/135) 交付该状态迁移；最终 head、短文档 CI 和实际 merge commit 以该 PR 的 GitHub 记录为准。

## 2026-08-14 - 修复 nanoid 基线依赖漏洞

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-14；用户要求修复阻塞 PR #137 的基线 CI 问题并提供返工指导）
- 方案版本：2026-08-14 本月条目
- 执行路线：直接 main，短期依赖分支 `chore/pin-nanoid-3.3.18`

### 确认方案

- PR #137 的 frontend job 被 `GHSA-2v37-7h3g-55p8` 阻断；该 PR 未修改 `package.json` 或 `pnpm-lock.yaml`，失败来自 `main` 的根级 pnpm override 将 `nanoid` 固定为仍受影响的 `3.3.17`。
- GitHub Advisory 标明 `nanoid` 3.x 的首个修复版本为 `3.3.18`。最小修复把 `pnpm-workspace.yaml` 的安全版本例外和 override，以及 `pnpm-lock.yaml` 的 override、package resolution、snapshot 和 `postcss` 依赖解析统一到 `3.3.18`；不改业务代码、`package.json`、审计策略或漏洞 allowlist。
- 仓库 checkout 禁止安装依赖或运行 pnpm；本地只运行 cloud-only 合同、自测、变更 Node 文件语法检查和 `git diff --check`。依赖安装、审计、frontend/Rust 检查由短期 PR 的自动 CI 完成。
- 基线修复必须先独立合并到 `main`。随后 PR #137 的执行 session 只同步最新 `origin/main`、解决实际冲突、更新 `delivery.md`，等待新 head 的必需检查绿色并重新交付；不得把安全依赖修复重复提交到功能分支。

### 实施结果

- 基线修复 PR [#140](https://github.com/KNaiFen/aio-coding-hub/pull/140) 已将 `pnpm-workspace.yaml` 的安全版本例外和 override、`pnpm-lock.yaml` 的 override、package resolution、snapshot 与 `postcss` 解析统一到 `nanoid 3.3.18`；未修改业务代码、`package.json` 或依赖审计 allowlist。
- npm registry tarball 的 SHA-512 已与锁文件 integrity 精确比对；本地 cloud-only 合同、自测和 `git diff --check` 通过，未安装依赖或运行 pnpm、构建、产品测试、Cargo 或 Tauri。
- PR #140 的最终 head `abe221420d597a4afed873f85e75686ddb72fce7` 在自动 [CI run 31752575312](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31752575312) 上通过 frozen install、pnpm 依赖审计、frontend、Rust、docs/support contract 和 `ci-gate`，CodeQL 与 `pr-title` 也通过；实际 squash merge commit 为 `a0f6823be399b2a2a31fb27b9fdfb0b6cb60e885`，本地 `main` 已快进并与 `origin/main` 同步。
- PR #137 的冻结 head `bbe3e8bb96ef09cdff6b791b7ee4d1d9c29b9f4d` 已由 main 记录 Round 1 finding；返工指导提交为 `1ed9c378da2730a052cd6bc869600b90372bf6cd`。执行 session 的唯一写权已恢复，下一步是普通 merge 最新 `origin/main`、等待新 head 的完整自动 CI、更新 `delivery.md` 并标记 Ready 后再次交给 main 验收。

## 2026-08-14 - 发布 AIO Coding Hub 0.60.52

- 状态：完成
- 负责人：main session
- 实施授权：已确认（2026-08-14；用户明确要求发版）
- 方案版本：2026-08-14 本月条目
- 执行路线：直接 main，短期版本分支 `chore/release-0.60.52`

### 确认方案

#### 问题与目标

- 当前正式版本为 `0.60.51`，其发布 tag `aio-coding-hub-v0.60.51` 已存在；本次按 fork 规则递增 patch 到 `0.60.52`。
- 发布内容包含 `0.60.51` 以来已合入 main 的跨供应商模型路由、TUI 请求摘要、依赖安全基线和任务交付收尾；版本提交只同步版本源、顶部 CHANGELOG 条目和本条记录，不修改产品行为。
- 发布必须先通过版本 PR 的自动检查，再等待该精确 main merge SHA 的完整候选 CI，最后创建指向该 SHA 的 annotated tag，由 release workflow 只晋升已验证候选并发布正式 Release。

#### 范围与锁定决定

- 仓库修改限定为 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/crates/aio-observer-protocol/Cargo.toml`、`src-tauri/crates/aio-tui/Cargo.toml`、`src-tauri/Cargo.lock` 中三个 workspace 包版本，以及 `CHANGELOG.md` 和本条记录。
- 所有版本源必须为 `0.60.52`；tag 必须为 `aio-coding-hub-v0.60.52`，且 annotated tag 解引用后的提交必须是 `origin/main` 祖先。
- `PENDING.md` 当前无 `pending` 或 `planned` 项；本次不新增或处置待办。
- 不安装依赖，不在本地运行 package-manager、Cargo、Tauri、签名、打包、构建或产品测试；完整验证和候选制品由 GitHub Actions 承担。

#### 验收与验证

- 版本 PR 的最新 head 必须通过 `ci-gate`、`pr-title` 及其按变更范围选中的检查；main 合并后必须记录实际 merge SHA。
- 只接受绑定该 merge SHA 的唯一未过期 `release-candidate-<sha>-<run-id>-<attempt>` artifact；候选 CI 必须包含 Windows x64、macOS ARM64 签名桌面、四平台 TUI 和聚合校验文件。
- annotated tag 推送后，release workflow 必须验证版本一致性、main 祖先关系、12 个资产、`latest.json` 和 `SHA256SUMS.txt`，且不重建或覆盖既有 Release。

#### 风险与停止条件

- 版本源不一致、PR head/merge SHA 漂移、候选缺失/过期/重复、CI 失败、tag 冲突或资产/checksum 不完整时停止发布并记录证据。
- 长时 CI/Release 监控按 3 分钟间隔、以 1 小时为截止时间；明确终态、失败、head 漂移或其他事件可立即复查。

### 实施结果

- 版本同步：`package.json`、Tauri/Cargo 版本源、`Cargo.lock` 中三个 workspace 包和 `CHANGELOG.md` 已统一为 `0.60.52`；本地依赖无关 release/source/TUI/cloud-only/Markdown 合同检查与 `git diff --check` 均通过，未安装依赖或运行产品构建、测试、Cargo、Tauri、签名和打包。
- 版本 PR：[PR #144](https://github.com/KNaiFen/aio-coding-hub/pull/144) 的最终 head 为 `dcd1a1f9c1cdb598d608348b261568d7d7ff15c9`，自动 `ci-gate`、`pr-title`、frontend、Rust、docs/support contract 和 CodeQL 均通过；实际 squash merge commit 为 `5c0344fdbdbe9db0e8ce389cdf2c3604ac06f248`。
- main 候选 CI：[run 31778841241](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31778841241) 的 attempt 1 始终绑定 merge SHA `5c0344fdbdbe9db0e8ce389cdf2c3604ac06f248`；frontend、Rust、Windows x64 与 macOS ARM64 签名桌面、四平台 TUI、`assemble-release-candidate` 和 `ci-gate` 全部成功。
- 候选制品：唯一未过期的聚合候选为 `release-candidate-5c0344fdbdbe9db0e8ce389cdf2c3604ac06f248-31778841241-1`（artifact `9211934485`）；候选含 12 个发布文件，`SHA256SUMS.txt` 对其余 11 个资产全部校验通过，`latest.json` 为版本 `0.60.52` 并包含 `windows-x86_64`、`darwin-aarch64` 的签名更新信息。
- 正式发布：创建并推送 annotated tag `aio-coding-hub-v0.60.52`，tag 对象为 `f43857eef316d0a7328e25a31e63e02633cfeb71`，解引用后精确指向 merge SHA `5c0344fdbdbe9db0e8ce389cdf2c3604ac06f248`。自动 [Release workflow run 31782034780](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31782034780) 成功，只晋升已验证候选；正式 [GitHub Release](https://github.com/KNaiFen/aio-coding-hub/releases/tag/aio-coding-hub-v0.60.52) 已公开，非 draft、非 prerelease。
- 发布后验证：正式 Release 的 12 个资产全部为 `uploaded`；重新下载后，正式 `SHA256SUMS.txt` 与候选逐字一致并对 11 个实际资产全部验证通过，`latest.json` 的版本及两个平台下载 URL 均正确指向 `aio-coding-hub-v0.60.52`。
- 范围外与后续：`PENDING.md` 当前无未解决条目；Homebrew tap 是正式 Release 之后的独立手工步骤，本次未执行，也未将其计入发布完成条件。

## 2026-08-14 - 发布 AIO Coding Hub 0.60.53

- 状态：完成（2026-08-15，Asia/Shanghai）
- 负责人：main session
- 实施授权：已确认（2026-08-14；用户明确要求清理、归档、发版并整理文档）
- 方案版本：2026-08-14 本月条目
- 执行路线：直接 main，短期版本分支 `chore/release-0.60.53`

### 确认方案

#### 问题与目标

- 当前正式版本为 `0.60.52`，其发布 tag `aio-coding-hub-v0.60.52` 已存在；本次按 fork 规则递增 patch 到 `0.60.53`。
- 发布内容包含 `0.60.52` 以来已合入 main 的冗余测试与 CI 合同精简、TUI 活跃请求实时持续时间恢复、CLI 监听重绑死锁修复、LAN token 一次性展示时序修复，以及两项任务的归档收尾。
- 版本提交只同步版本源、顶部 CHANGELOG 条目和本条记录，不新增产品行为；发布必须先通过版本 PR，再复用绑定该精确 main merge SHA 的完整候选制品，最后由 release workflow 晋升而不重建。

#### 范围与锁定决定

- 仓库修改限定为 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/crates/aio-observer-protocol/Cargo.toml`、`src-tauri/crates/aio-tui/Cargo.toml`、`src-tauri/Cargo.lock` 中三个 workspace 包版本，以及 `CHANGELOG.md` 和本条记录。
- 所有版本源必须为 `0.60.53`；tag 必须为 `aio-coding-hub-v0.60.53`，且 annotated tag 必须精确指向版本 PR 的 main merge SHA。
- `PENDING.md` 当前无 `pending` 或 `planned` 项；本次不新增或处置待办。Homebrew tap 仍是正式 Release 之后的独立范围，不计入本次发布完成条件。
- 不安装依赖，不在本地运行 package-manager、Cargo、Tauri、签名、打包、构建、生成或产品测试；完整验证和候选制品由 GitHub Actions 承担。

#### 验收与验证

- 版本 PR 的最新 head 必须通过 `ci-gate`、`pr-title`、frontend、Rust、contracts 和 CodeQL；main 合并后必须记录实际 merge SHA。
- 只接受绑定该 merge SHA 的唯一未过期 `release-candidate-<sha>-<run-id>-<attempt>` artifact；候选 CI 必须包含 Windows x64、macOS ARM64 签名桌面、四平台 TUI、聚合候选和 `ci-gate`。
- 候选必须包含 12 个发布文件，`SHA256SUMS.txt` 必须校验其余 11 个资产，`latest.json.version` 必须为 `0.60.53`；annotated tag 推送后 release workflow 只能晋升该候选。
- 正式 Release 必须非 draft、非 prerelease，12 个资产全部 uploaded；重新下载后 checksum 和 `latest.json` 下载 URL 必须再次验证。

#### 风险与停止条件

- 版本源不一致、PR head/merge SHA 漂移、候选缺失/过期/重复、CI 失败、tag 冲突、签名缺失或资产/checksum 不完整时停止发布并记录证据。
- 长时 CI/Release 监控按 3 分钟间隔、以 1 小时为截止时间；明确终态、失败、head 漂移或其他事件可立即复查。

### 实施结果

- 归档与清理：功能 PR #147 的最终接受 head `a9dd8288285c0149c3cd58315a7ac5c602488755` 已通过完整验收并以 `da7ec35b31b44019432f6cdb61dee19bf84fc397` 合入；原执行 worktree、任务分支均已删除。records-only [PR #149](https://github.com/KNaiFen/aio-coding-hub/pull/149) 将任务包归档并以 `f273d1aa6d46fd3432b2be3ccc9308e0e8df83b7` 合入，process-docs 范围的 `ci-gate`、`pr-title` 和 CodeQL 全绿，产品构建按设计跳过。
- 版本同步：`package.json`、Tauri/Cargo 版本源、`Cargo.lock` 中三个 workspace 包和 `CHANGELOG.md` 已统一为 `0.60.53`。本地 release version/source/promotion、TUI release、cloud-only、Markdown links 和 `git diff --check` 全部通过；未安装依赖或运行 package-manager、Cargo、构建、生成、Tauri、签名、打包和产品测试。
- 版本 PR：[PR #150](https://github.com/KNaiFen/aio-coding-hub/pull/150) 的最终 head 为 `03c948134d6359132d029beca6e75ca936578632`。自动 [CI run 31809382647](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31809382647) attempt 1 唯一失败为未被版本 diff 修改的 `cross_provider_sse_success_keeps_target_marker_and_does_not_bind_session` 偶发得到 `502`；同一产品代码的前一 main run 已明确通过该测试，因此未修改或放宽范围外路由测试，只重跑失败 jobs。attempt 2 的 Rust、`ci-gate` 与既有 frontend、contracts、CodeQL、`pr-title` 全部通过；实际 squash merge commit 为 `16f3bbbf38a907cca4a809cd733352b1cdc0ea8f`。
- main 候选 CI：[run 31813699622](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31813699622) 的 attempt 1 始终绑定 merge SHA `16f3bbbf38a907cca4a809cd733352b1cdc0ea8f`；frontend、Rust、Windows x64 与 macOS ARM64 签名桌面、四平台 TUI、`assemble-release-candidate` 和 `ci-gate` 全部成功。
- 候选制品：唯一未过期的聚合候选为 `release-candidate-16f3bbbf38a907cca4a809cd733352b1cdc0ea8f-31813699622-1`（artifact `9225478474`）；候选含 12 个发布文件，`SHA256SUMS.txt` 对其余 11 个资产全部校验通过，`latest.json.version` 为 `0.60.53`，并包含 `windows-x86_64`、`darwin-aarch64` 的签名更新信息和正确 tag URL。
- 正式发布：annotated tag `aio-coding-hub-v0.60.53` 的 tag object 为 `9ac79983ab50664c50c216567ce2e30149538291`，解引用后精确指向 merge SHA `16f3bbbf38a907cca4a809cd733352b1cdc0ea8f`。自动 [Release workflow run 31817535866](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31817535866) 成功，只晋升已验证候选；正式 [GitHub Release](https://github.com/KNaiFen/aio-coding-hub/releases/tag/aio-coding-hub-v0.60.53) 已公开，非 draft、非 prerelease。
- 发布后验证：正式 Release 的 12 个资产全部为 `uploaded`；重新下载后，正式 `SHA256SUMS.txt` 与候选逐字一致并对 11 个实际资产全部验证通过，正式 `latest.json` 也与候选逐字一致，版本及两个平台下载 URL 均正确指向 `aio-coding-hub-v0.60.53`。两个仓库外临时下载目录已删除，本地 checkout 未留下制品。
- 文档、PENDING 与范围外：长期行为已由 `gateway-listen-token-contract.md`、`local-observer-tui-contract.md` 和 cross-layer index 覆盖，`docs/README.md` 无需新增重复入口；`PENDING.md` 当前无未解决条目。Homebrew tap 是正式 Release 之后的独立手工范围，本次未执行，也未计入发布完成条件。

## 2026-08-16 - 收敛 CI 监控与本地验证 Skill

- 状态：完成
- 负责人：main session
- PR：[PR #155](https://github.com/KNaiFen/aio-coding-hub/pull/155)
- 实施授权：已确认（2026-08-16；用户要求消除重复 Skill，并新增 `gkd-ci-monitor`、`gkd-local-verify`，将对应 AGENTS 规则下沉并提供固定脚本）
- 方案版本：2026-08-16 本月条目
- 执行路线：直接 main，短期分支 `chore/gkd-ci-local-skills`
- 迁移记录：不适用

### 确认方案

#### 问题与目标

- 消除 `~/.codex` 与 `~/.agents` 对 `terminal-ops`、`verification-loop` 的重复发现，只保留更适配 Codex 的 `~/.codex` 版本。
- 新增短小、自动路由的 `gkd-ci-monitor` 和 `gkd-local-verify`；把可按需加载的 CI 轮询、本地验证步骤从 AGENTS 下沉，并把易错判定固化为脚本。
- 保留权限、唯一写者、固定 head、cloud-only 禁令等加载 Skill 前就必须生效的硬边界。

#### 范围

- 必须完成：清理重复 Skill 与锁记录；创建两个 `~/.codex/skills/gkd-*` Skill；新增仓库固定本地验证入口及 selftest；同步角色路由、AGENTS、现行合同和用户文档。
- 允许修改：上述用户 Skill、`~/.codex/AGENTS.md`、仓库 `AGENTS.md`、`scripts/`、CI 机器合同、README、Trellis/cloud-only 规范和本条记录。
- 不在范围内：产品行为、依赖、构建流程、Rust/Tauri 代码、额外 hook 或复杂门禁。

#### 用户锁定决定

- 所有新增 Skill 使用 `gkd-` 前缀并只安装到 `~/.codex/skills/`。
- 结构化或易错操作优先交给 CLI/脚本；不让模型手写状态 JSON、长交付清单或自由选择本地重检查。
- 不引入复杂门禁；AGENTS 保持短，只常驻权限和首次搜索前必须生效的边界。

#### 事实、假设与未决问题

| 项目 | 来源 | 状态或关闭条件 |
|---|---|---|
| `~/.agents` 两个同名 Skill 是独立 ECC 文件而非链接；`.skill-lock.json` 有对应安装记录 | 本机文件与哈希核验 | 已确认，目录与锁项一并移除 |
| 本仓库本地重检查属于 cloud-only，现有通用 `verification-loop` 会建议 build/lint/test | 现行合同与 Skill 原文 | 已确认，新 Skill 只能调用仓库固定白名单脚本 |
| PENDING 当前无 `pending` 或 `planned` 条目 | `PENDING.md` | 已审阅，无需纳入 |

无材料性未决问题。

#### 实现方向

- `gkd-ci-monitor` 使用只读脚本校验完整 head、必需检查与指定 check 名称，按 180-300 秒间隔轮询，并以成功、失败、head 漂移、超时四种封闭终态退出。
- `gkd-local-verify` 只调用仓库 `check-local-verification.mjs --base <full-sha>`；runner 使用硬编码、无 shell 的命令白名单，覆盖合同/selftest、提交与工作区 diff、变更 Node 文件语法。
- 全局 AGENTS 仅保留长时 CI 子代理例外及 Skill 路由；项目 AGENTS 保留 cloud-only 禁令和云端所有权，把具体本地命令收敛为单一入口。

#### 验收与验证

- AC-01：新会话技能发现中两个通用 Skill 各只有 `~/.codex` 一份，`~/.agents/.skill-lock.json` 无残留项。
- AC-02：两个新 Skill 通过 `quick_validate.py`，脚本自测覆盖成功、失败、漂移、超时或参数拒绝等关键分支。
- AC-03：本地 runner 不接受命令透传，不解析 package scripts，不运行依赖、package manager、构建、测试、Cargo 或 Tauri；实际允许的合同、自测、`node --check`、`git diff --check` 通过。
- AC-04：AGENTS、角色 Skill、README、cloud-only 规范和 CI 合同对新入口一致，无旧的手工双命令文档偏移。

#### 风险与停止条件

- GitHub CLI JSON 结构、ruleset/required checks 含义或仓库基准 SHA 无法确定时停止，不能把未知状态判成绿色。
- 删除范围仅限两个已核验目录及锁项；发现其他客户端的路径级依赖时停止。
- PENDING 审阅：当前无相关项。

#### 范围与决定变更

| 日期 | 原/新范围或决定 | 影响的 AC/验证 | 决定人和恢复条件 |
|---|---|---|---|
| 无 |  |  |  |

### 实施结果

- 结果：仓库与用户 Skill 实施完成，最终固定 head 验收、自动 CI 和同步合并均完成。
- 代码与文档位置：`~/.codex/skills/gkd-ci-monitor` 提供固定 head 只读轮询和 7 项分类自测；`~/.codex/skills/gkd-local-verify` 路由仓库 `scripts/check-local-verification.mjs`；runner 与 `check-local-verification.selftest.mjs` 固化 base/变更面解析、合同、自测、diff、未跟踪文本空白和 Node 语法检查。全局/项目 AGENTS、三个角色 Skill、Trellis workflow/spec、README、Actions 与两项机器合同已同步。
- 验收结果：AC-01 通过，`~/.agents` 两个重复目录和 `.skill-lock.json` 锁项均已消除，`~/.codex` 版本保留；AC-02 通过脚本自测、非法参数、真实 GitHub 失败路径和无依赖 YAML 元数据解析；AC-03 runner 在基准 `e3d79e1f177f495c747474269d17ce2d0a6dbd66` 上返回 `local_ready` 并自动检查 7 个变更 Node 文件，所有重检查明确为 `cloud_owned`；AC-04 cloud-only/CI quality 正反合同均通过，现行文档扫描没有残留旧双命令或手写轮询入口。
- 方案偏移：`skill-creator` 的官方 `quick_validate.py` 已实际调用，但本机 Python 缺少校验器自身所需的 `yaml` 模块；未为此安装依赖，改用系统 Ruby/Psych 无依赖解析两份 Skill frontmatter 与 `agents/openai.yaml`。首次本地 Skill 前向测试发现 cloud-only checker 仍锁定旧 Trellis 句子，已同步机器断言并新增负例后重新通过。
- 验证：`gkd-ci-monitor` 7 项 Python selftest 通过，非法 PR 参数按 JSON/退出码 4 拒绝；真实读取已合并 PR #154 时，同一预期/实际 head 返回 `failure`/退出码 1 且未修改 GitHub。两份 Skill 元数据、目录名、默认 prompt 和无 TODO 由 Ruby/Psych 校验通过。仓库 `check-local-verification.selftest.mjs`、cloud-only selftest/production、CI quality selftest/production、7 个变更 Node 文件 `node --check` 和固定 runner 全部通过。PR #155 最终 head `bda4aa2855c591484696a163fb63ab0856782199` 的 [ci-gate](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31955873464/job/95189107938)、[pr-title](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31955873299/job/95186319458)、contracts、frontend、Rust 和 CodeQL 均成功，候选发布 jobs 按 PR 边界跳过；监控脚本确认实际 head 未漂移后才允许合并。未安装依赖或在本地运行 package manager、格式化、lint、类型检查、产品测试、构建、Cargo、Tauri、签名或打包。
- 交付证据：首个实现提交 `189ece816a01b9cc91553fbc9f333b4921e4b8e4`；[PR #155](https://github.com/KNaiFen/aio-coding-hub/pull/155) 的最终接受 head 为 `bda4aa2855c591484696a163fb63ab0856782199`，expected-head squash merge commit 为 `c79f0bc64bd4e0f9774f2bf7332fbae548ead6d3`。
- 文档与合同：已更新 `AGENTS.md`、`.trellis/workflow.md`、三份现行 cross-layer spec、`README.md`/`README_EN.md`、`docs/README.md`、`ci.yml`、cloud-only/CI quality checker 及 selftest。历史任务、历史审计和旧交付证据按时点保留。
- 遗留事项：当前会话的启动时 Skill 清单仍可能显示旧的重复发现快照；新开 Codex 会话后才会按当前磁盘重新发现。除此之外无已知本地遗留。
- 收尾：本地 `main` 已快进到 `c79f0bc64bd4e0f9774f2bf7332fbae548ead6d3` 并与 `origin/main` 同步；远端和本地功能分支均已删除，无 sibling worktree。生成的 `__pycache__` 已移入系统废纸篓且 Skill 目录无残留；PENDING 无条目需要迁移，用户可见产品/API/架构无变化。

## 2026-08-17 - 发布 AIO Coding Hub 0.60.54

- 状态：完成（2026-08-17，Asia/Shanghai）
- 负责人：main session
- 实施授权：已确认（2026-08-17；用户要求在 TUI 任务通过审查后直接合并并发版）
- 方案版本：2026-08-17 本月条目
- 执行路线：直接 main，短期版本分支 `chore/release-0.60.54`

### 确认方案

#### 问题与目标

- 当前正式版本为 `0.60.53`，对应 tag 和 Release 均已存在；本次按 fork 规则递增 patch 到 `0.60.54`。
- 发布内容包含 `0.60.53` 以来已合入 main 的多 worktree/session 协作流程精简、固定 head CI 与零产物本地验证合同，以及 TUI 压缩模式、跨供应商模型路由、route outcome/指标一致性和窄面板供应商可用性布局修复。
- 版本提交只同步版本源、顶部 CHANGELOG、本条记录和已完成的 TUI 归档清理事实，不新增产品行为；正式发布只晋升绑定版本 PR main merge SHA 的现有候选制品，不在 release workflow 重建。

#### 范围与锁定决定

- 仓库修改限定为 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/crates/aio-observer-protocol/Cargo.toml`、`src-tauri/crates/aio-tui/Cargo.toml`、`src-tauri/Cargo.lock` 中三个 workspace 包版本、`CHANGELOG.md`、本条记录，以及 `.trellis/tasks/archive/2026-08/08-17-tui-observability-consistency/acceptance.md` 的实际清理结果。
- 所有版本源必须为 `0.60.54`；tag 必须为 `aio-coding-hub-v0.60.54`，且 annotated tag 必须精确指向版本 PR 的 main merge SHA。
- `PENDING.md` 当前无 `pending` 或 `planned` 项；本次不新增或处置待办。Homebrew tap 是正式 Release 之后的独立手工范围，不计入本次发布完成条件。
- 不安装依赖，不在本地运行 package-manager、Cargo、Tauri、签名、打包、构建、生成或产品测试；完整验证和候选制品由 GitHub Actions 承担。

#### 验收与验证

- 版本 PR 的最新 head 必须通过 `ci-gate`、`pr-title`、frontend、Rust、contracts 和 CodeQL；任何新提交都会使旧结论失效。
- 只接受绑定版本 PR main merge SHA 的唯一未过期 `release-candidate-<sha>-<run-id>-<attempt>` artifact；候选 CI 必须包含 Windows x64、macOS ARM64 签名桌面、四平台 TUI、聚合候选和 `ci-gate`。
- 候选必须包含 12 个发布文件，`SHA256SUMS.txt` 必须校验其余 11 个资产，`latest.json.version` 必须为 `0.60.54`；annotated tag 推送后 release workflow 只能晋升该候选。
- 正式 Release 必须非 draft、非 prerelease，12 个资产全部 uploaded；重新下载后 checksum 和 `latest.json` 下载 URL 必须再次验证。

#### 风险与停止条件

- 版本源不一致、PR head/merge SHA 漂移、候选缺失/过期/重复、CI 失败、tag 冲突、签名缺失或资产/checksum 不完整时停止发布并记录证据。
- 长时 PR CI 使用 `$gkd-ci-monitor` 固定 head；main 候选与 Release workflow 以 3 分钟间隔、1 小时为截止时间，只在明确终态、失败或输入漂移时结束等待。

### 实施结果

- 归档与清理：TUI 功能 PR #157 的固定接受 head `03d9a3cf00bc1b030ead056a4056ac62c3094481` 已通过审查并以 `20c19d1e05b95a0939c3d925b81e7d8b704b7ec8` 合入；records-only PR #159 以 `4db873066a5e50f740ba35fd06c95fa21ff182bf` 合入。原 TUI worktree 与两个本地短期分支已删除，对应远端分支由 GitHub 自动删除。
- 版本同步：`package.json`、Tauri/Cargo 版本源、`Cargo.lock` 三个 workspace 包和 `CHANGELOG.md` 已统一为 `0.60.54`；固定本地 runner 以 base `4db873066a5e50f740ba35fd06c95fa21ff182bf` 返回 `local_ready`，未安装依赖或运行 package-manager、Cargo、构建、生成、Tauri、签名、打包和产品测试。
- 版本 PR：[PR #160](https://github.com/KNaiFen/aio-coding-hub/pull/160) 的最终固定 head 为 `c9565d448521b99f49f4f928ae38033add243d49`，`ci-gate`、`pr-title`、frontend、Rust、contracts 和 CodeQL 全部通过；按 expected head squash 合入后的 main merge SHA 为 `fb6da0e9586c3e65d6f945112a9f62e36312641a`。
- main 候选 CI：[run 31972186789](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31972186789) 的 attempt 1 始终绑定 merge SHA `fb6da0e9586c3e65d6f945112a9f62e36312641a`；candidate plan、contracts、frontend、Rust、Windows x64 与 macOS ARM64 签名桌面、四平台 TUI、`assemble-release-candidate` 和 `ci-gate` 全部成功。
- 候选制品：唯一未过期的聚合候选为 `release-candidate-fb6da0e9586c3e65d6f945112a9f62e36312641a-31972186789-1`（artifact `9270629450`）；候选含 12 个发布文件，`SHA256SUMS.txt` 对其余 11 个资产全部校验通过，`latest.json.version` 为 `0.60.54`，并包含 `windows-x86_64`、`darwin-aarch64` 的签名更新信息和正确 tag URL。
- 正式发布：annotated tag `aio-coding-hub-v0.60.54` 的 tag object 为 `2ec5bb41eb1ea983df68a27d23a8636240a9e19c`，解引用后精确指向 merge SHA `fb6da0e9586c3e65d6f945112a9f62e36312641a`。自动 [Release workflow run 31974458402](https://github.com/KNaiFen/aio-coding-hub/actions/runs/31974458402) 成功，只晋升已验证候选；正式 [GitHub Release](https://github.com/KNaiFen/aio-coding-hub/releases/tag/aio-coding-hub-v0.60.54) 已公开，非 draft、非 prerelease。
- 发布后验证：正式 Release 的 12 个资产全部为 `uploaded`；重新下载后，正式 `SHA256SUMS.txt` 与候选逐字一致并对 11 个实际资产全部验证通过，正式 `latest.json` 也与候选逐字一致，版本及两个平台下载 URL 均正确指向 `aio-coding-hub-v0.60.54`。两个仓库外临时下载目录已删除，本地 checkout 未留下制品。
- 文档、PENDING 与范围外：本次用户可见行为已由 TUI 任务设计、验收和归档记录覆盖，未新增重复长期文档入口；`PENDING.md` 当前无未解决条目。Homebrew tap 是正式 Release 之后的独立手工范围，本次未执行，也未计入发布完成条件。

## 2026-08-17 - 发布 AIO Coding Hub 0.60.55

- 状态：完成（2026-08-17，Asia/Shanghai）
- 负责人：main session
- 实施授权：已确认（2026-08-17；用户明确要求合并 Codex 过载错误码改写的归档 PR 后发版）
- 方案版本：2026-08-17 本月条目
- 执行路线：直接 main，短期版本分支 `chore/release-0.60.55`

### 确认方案

#### 问题与目标

- 当前正式版本为 `0.60.54`，对应 tag 和 Release 均已存在；本次按 fork 规则递增 patch 到 `0.60.55`。
- 发布内容限定为 `0.60.54` 以来已合入 main 的 Codex Responses 过载错误码兼容改写：用户可在 CLI 管理 > Codex 中按需开启，使第三方 OpenAI-compatible 中转的 `server_is_overloaded`/`slow_down` 在下游视图成为 `server_error`，同时保留上游原始日志证据和有界 fail-open 行为。
- 版本提交只同步版本源、顶部 CHANGELOG、本条记录和已完成任务的实际清理结果，不新增其他产品行为；正式发布只晋升绑定版本 PR main merge SHA 的现有候选制品，不在 release workflow 重建。

#### 范围与锁定决定

- 仓库修改限定为 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/crates/aio-observer-protocol/Cargo.toml`、`src-tauri/crates/aio-tui/Cargo.toml`、`src-tauri/Cargo.lock` 中三个 workspace 包版本、`CHANGELOG.md`、本条记录，以及 `.trellis/tasks/archive/2026-08/08-17-codex-sse-overload-retry-rewrite/acceptance.md` 的实际清理结果。
- 所有版本源必须为 `0.60.55`；tag 必须为 `aio-coding-hub-v0.60.55`，且 annotated tag 必须精确指向版本 PR 的 main merge SHA。
- `PENDING.md` 当前无 `pending` 或 `planned` 项；本次不新增或处置待办。Homebrew tap 是正式 Release 之后的独立手工范围，不计入本次发布完成条件。
- 不安装依赖，不在本地运行 package-manager、Cargo、Tauri、签名、打包、构建、生成或产品测试；完整验证和候选制品由 GitHub Actions 承担。

#### 验收与验证

- 版本 PR 的最终固定 head 必须通过 `ci-gate`、`pr-title`、frontend、Rust、contracts 和 CodeQL；任何新提交都会使旧结论失效。
- 版本 PR 合并后，只接受绑定其 main merge SHA 的唯一未过期 `release-candidate-<sha>-<run-id>-<attempt>` artifact；main CI 必须包含 Windows x64、macOS ARM64 签名桌面、四平台 TUI、聚合候选和 `ci-gate`。
- 候选必须包含 12 个发布文件，`SHA256SUMS.txt` 必须校验其余 11 个资产，`latest.json.version` 必须为 `0.60.55`；annotated tag 推送后 release workflow 只能晋升该候选。
- 正式 Release 必须非 draft、非 prerelease，12 个资产全部 uploaded；重新下载后 checksum 和 `latest.json` 下载 URL 必须再次验证。

#### 风险与停止条件

- 版本源不一致、PR head/merge SHA 漂移、候选缺失/过期/重复、CI 失败、tag 冲突、签名缺失或资产/checksum 不完整时停止发布并记录证据。
- 长时 PR CI 使用 `$gkd-ci-monitor` 固定 head；main 候选与 Release workflow 以 3 分钟间隔、1 小时为截止时间，只在明确终态、失败或输入漂移时结束等待。

### 实施结果

- 归档与清理：Codex 功能 PR #164 已以 `fabc7babee7230eede254127b09fa62e454f6f78` 合入，records-only PR #165 已以 `f558c5db6ec8adb508c22766c5f7a5a59f332baf` 合入；原任务 worktree 与三个本地短期分支已删除，对应远端分支由 GitHub 自动删除。
- 版本同步：`package.json`、Tauri/Cargo 版本源、`Cargo.lock` 三个 workspace 包和 `CHANGELOG.md` 已统一为 `0.60.55`；固定本地 runner 以 base `f558c5db6ec8adb508c22766c5f7a5a59f332baf` 返回 `local_ready`，未安装依赖或运行 package-manager、Cargo、构建、生成、Tauri、签名、打包和产品测试。
- 版本 PR：[PR #166](https://github.com/KNaiFen/aio-coding-hub/pull/166) 的最终固定 head 为 `37a068a27a980506d1ce9fc5b0ea02a92ad063a2`，`ci-gate`、`pr-title`、frontend、Rust、contracts 和 CodeQL 全部通过；按 expected head squash 合入后的 main merge SHA 为 `00f6abb115e185d379dd9c587a5f8f7e52eb6f53`。本地版本分支已删除，对应远端分支由 GitHub 自动删除。
- main 候选 CI：[run 32016226909](https://github.com/KNaiFen/aio-coding-hub/actions/runs/32016226909) 的 attempt 1 始终绑定 merge SHA `00f6abb115e185d379dd9c587a5f8f7e52eb6f53`；candidate plan、contracts、frontend、Rust、Windows x64 与 macOS ARM64 签名桌面、四平台 TUI、`assemble-release-candidate` 和 `ci-gate` 全部成功。
- 候选制品：唯一未过期的聚合候选为 `release-candidate-00f6abb115e185d379dd9c587a5f8f7e52eb6f53-32016226909-1`（artifact `9284789117`）；候选含 12 个发布文件，`SHA256SUMS.txt` 对其余 11 个资产全部校验通过，`latest.json.version` 为 `0.60.55`，并包含 `windows-x86_64`、`darwin-aarch64` 的签名更新信息和正确 tag URL。
- 正式发布：annotated tag `aio-coding-hub-v0.60.55` 的 tag object 为 `d2a41fd548a3807f11565d74df98562dac855c27`，解引用后精确指向 merge SHA `00f6abb115e185d379dd9c587a5f8f7e52eb6f53`。自动 [Release workflow run 32019919310](https://github.com/KNaiFen/aio-coding-hub/actions/runs/32019919310) 成功，只晋升已验证候选；正式 [GitHub Release](https://github.com/KNaiFen/aio-coding-hub/releases/tag/aio-coding-hub-v0.60.55) 已公开，非 draft、非 prerelease。
- 发布后验证：正式 Release 的 12 个资产全部为 `uploaded`；重新下载后，正式 `SHA256SUMS.txt` 与候选逐字一致并对 11 个实际资产全部验证通过，正式 `latest.json` 也与候选逐字一致，版本及两个平台下载 URL 均正确指向 `aio-coding-hub-v0.60.55`。两个仓库外临时下载目录已删除，本地 checkout 未留下制品。
- 文档、PENDING 与范围外：本次用户可见行为已由 Codex SSE 改写任务的设计、验收和归档记录覆盖，未新增重复长期文档入口；`PENDING.md` 当前无未解决条目。Homebrew tap 是正式 Release 之后的独立手工范围，本次未执行，也未计入发布完成条件。

## 2026-08-23 - AIO GKD Consumer Policy Anchor

- 状态：完成
- 负责人：trusted main
- 实施授权：2026-08-23 用户授权完成 GKD 信任合同修订、安装已验收发行物并继续冻结的 AIO adoption 计划。
- 执行路线：手工 bootstrap anchor；在 `.gkd/policy.json` 合入 `main` 前，不启动 GKD automatic route 或创建 GKD task state。

### 确认方案

- 已发布并 restage 的输入为 GKD `v0.1.3`，bundle digest
  `cc465d26f08edb2a133775e4d6a58aa517eab1bde0ec2e1ec72f6d9f2c8883bd`。
- 新增严格 canonical `.gkd/policy.json`，只绑定 AIO 的 GitHub identity、`main` 与实际
  ruleset 的 `ci-gate`、`pr-title` 两项 required checks。
- 这是首次 consumer 的前置 policy anchor：R3 的 bootstrap 会从 future candidate base 读取并
  bind 该 policy；本 PR 不创建或伪造 task JSON、offer、claim、activation、receipt、delivery。

### 范围与停止条件

- 只修改 `.gkd/policy.json` 和本条 change record。
- 不修改 AIO 产品代码、CI workflow、release、legacy task lifecycle、生产 `~/.codex`、GKD
  canonical source、GitHub settings、Secrets 或 runner。
- policy/origin/base/ruleset 任一漂移、PR fixed-head CI 失败或独立审查发现阻塞项时停止；不得
  用 generic worker、手写协调状态或 AIO-local fork 继续。

### 验证与交付

- 本地只运行仓库既有零依赖 `scripts/check-local-verification.mjs`，完整前端、Rust、测试、
  审计、签名和打包仍由 GitHub Actions 负责。
- 合并前必须核对 exact PR head、`ci-gate` 和 `pr-title`，并独立审查 policy 的 canonical
  bytes、origin binding 与范围。合并后才可从已发布 `v0.1.3` asset 在 AIO project stage，随后
  建立正式 policy-bound GKD task；不在本 PR 创建 tag 或 Release。

### 实施结果

- 结果：完成。PR [#168](https://github.com/KNaiFen/aio-coding-hub/pull/168) 的固定 head
  `28ee93aab85fc7c03341a960b6a05fdbbac158ee` 只新增 `.gkd/policy.json` 并更新本条记录，已
  squash merge 为 `c19507f399a1dd7733de832a32ffa2926cd0b461`。
- policy 与远端：`.gkd/policy.json` 是 canonical JSON，绑定
  `github.com/KNaiFen/aio-coding-hub`、`main`、`ci-gate`、`pr-title`；其 policy digest 为
  `5fd82f880eb4f558142947651bbb6a35109c3bfbf151a11425ce3b19bc1c4317`。`origin` 与 active
  `Protect main` ruleset 的 required contexts 均与该 binding 一致。
- 验证与验收：相对基线 `4d6ee488c6e12ecc5735c865d5d786beb5b8e4f0` 的既有本地
  `node scripts/check-local-verification.mjs --base 4d6ee488c6e12ecc5735c865d5d786beb5b8e4f0` 返回 `local_ready`，且
  `git diff --check` 通过。PR #168 的 `ci-gate`、`pr-title` 在固定 head 均为 success。合并后
  的独立只读复核确认 policy canonical bytes、origin/ruleset 绑定和完整 diff 均无阻塞 finding；
  该复核是合并后的审计证据，不替代合并前的检查。
- project staging：从发布的 GKD `v0.1.3` asset 独立安装并验证后，`gkd-role project-stage`
  和 `gkd-role project-verify` 均成功。它们绑定 execution bundle digest
  `cc465d26f08edb2a133775e4d6a58aa517eab1bde0ec2e1ec72f6d9f2c8883bd`、上述 policy digest 和
  inventory digest `3c4a2700791b13e2e76519f101033774c7dd6bae9fae2435463d34594d1382bb`；runtime
  inventory 保持 machine-local 且由 `.git/info/exclude` 忽略。
- 边界：未修改 AIO 产品代码、CI workflow、release、legacy lifecycle、GitHub settings、Secrets
  或生产 `~/.codex`。后续工作只能以已合并 policy 为基础，创建新的正式 policy-bound GKD task；
  本次不创建 tag 或 Release。
