# 2026-09 简单变更记录

## 2026-09-02 — 移除旧 GKD 生命周期并统一 canonical bundle 入口

- 状态：实施中
- 负责人：main session
- PR：未创建
- 实施授权：已确认（2026-09-02；用户要求清除旧 GKD 不兼容工作流痕迹）
- 方案版本：2026-09-02 本月条目
- 执行路线：直接 main

### 确认方案

#### 问题与目标

- 当前仓库的现行文档、Trellis CLI、模板和 CI 仍把旧 `task.py` 生命周期当作 GKD 入口；这与 canonical bundle 的 `gkd-task`、`gkd-role`、`TrustedMainRuntimeBridge`、固定 head 验收和 `gkd_acceptor` 合同冲突。
- 完成后，仓库只保留 `.gkd` 项目 adapter 与产品文档；GKD 生命周期由已安装 bundle 提供，旧 Trellis GKD 脚本、入口和测试不再存在。

#### 范围

- 必须完成：删除旧 Trellis GKD 生命周期脚本/测试；更新根规则、工作流、操作文档、模板、README、active spec、CI checker 与错误提示；保留 AIO `.gkd` adapter 校验。
- 不在范围内：产品代码、Git 历史、`.trellis/tasks/archive/**` 历史正文、生产 `~/.codex` 安装和外部 GitHub 设置。

#### 用户锁定决定

- 不保留“legacy/historical”作为现行 GKD 契约；旧 `task.py` 不得再作为可执行入口或文档推荐。
- 不在仓库内复制或手写 `gkd-task`/`gkd-role` 实现；这些能力来自已安装 canonical bundle。

#### 验收与验证

- active 文档和规则只引用 canonical GKD 入口，不再出现 `task.py accept/start/delegate/deliver`。
- CI contracts 不再运行旧 Trellis Python 单测，并验证新的 GKD 文档入口与 `.gkd` adapter。
- 运行固定 `scripts/gkd-verify --base-sha <full-sha>`、`git diff --check` 及变更 Node 文件语法检查。

### 实施结果

- 结果：实施中。
- 遗留事项：完成修改和固定 runner 验证后补齐本节并单独提交。
