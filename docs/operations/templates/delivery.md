# 交付报告：{{任务名称}}

> 本文件描述 PR 当前实现，而不是复述原计划。执行 session 在最新 PR 提交通过必需 CI 后更新本文件并暂停，等待 main 验收。

## 交付状态

- 结果：{{等待验收 / 阻塞 / 失败}}
- PR：{{URL}}
- 分支：`{{branch}}`
- 最新提交：`{{HEAD，可选；main 仍以 PR 实时 head 为准}}`
- CI：{{必需检查已通过 / 未通过及原因 / 相关 job 按范围跳过}}
- 执行 session：{{已暂停 / 尚未暂停及原因}}

若结果不是“等待验收”，应在“未完成项与阻塞”中说明当前可保留成果和 main 需要决定的事项。

## 实现摘要

### 用户可见结果

- {{用户现在可以观察到什么变化。}}

### 内部实现

- {{核心机制、数据流、状态变化或接口行为。}}

## 验收标准对应

| 标准 | 结果 | 证据 |
|---|---|---|
| AC-01 {{名称}} | {{通过 / 未通过 / 待人工验证}} | {{测试、CI、人工步骤或代码位置}} |
| AC-02 {{名称}} |  |  |

不得把未运行的验证写成通过。需要 main 或用户完成的人工验收应明确标注。

## 主要代码位置

| 文件或符号 | 变更 | 设计原因 |
|---|---|---|
| `path/to/file:Symbol` | {{做了什么}} | {{为什么在这里实现}} |

列出理解实现所需的关键位置，不需要逐项枚举机械生成文件或完整 diff。

## 与计划的偏移

- {{无。}}
- {{若有：原计划、实际实现、改变原因、影响、main 是否需要确认。}}

执行中产生但未写入原方案的重要技术决定也记录在这里，并指出是否已经同步到现行规范或设计文档。

## 验证结果

### 本地检查

| 命令 | 结果 | 说明 |
|---|---|---|
| `git diff --check origin/main...HEAD` | {{通过 / 失败}} | 检查本分支已提交变更 |
| `{{命令}}` |  |  |

### GitHub CI 与编译

| Workflow / Job | 结果 | 链接或说明 |
|---|---|---|
| `ci-gate` | {{通过 / 未通过}} | {{URL}} |
| {{相关编译或构建}} | {{通过 / 按范围跳过 / 不适用}} |  |

### 人工验证

- {{无 / 环境、步骤、结果、脱敏证据。}}

## 测试、文档与合同

- 测试：{{新增或修改的位置；没有则解释为什么已有覆盖足够。}}
- 现行文档：{{更新位置 / 不适用。}}
- 类型或机器合同：{{更新位置 / 不适用。}}
- 迁移或发布说明：{{更新位置 / 不适用。}}

## 兼容性、风险与回滚

- 兼容性：{{无影响 / 具体影响和处理。}}
- 数据与配置：{{无影响 / 迁移、默认值、回退。}}
- 安全与隐私：{{无影响 / 边界变化。}}
- 回滚方式：{{回退提交即可 / 需要额外步骤。}}
- 剩余风险：{{无 / 具体风险、触发条件和缓解方式。}}

## 未完成项与阻塞

- {{无。}}
- {{未完成内容、原因、当前状态、需要 main/用户做出的决定。}}

不得把明确属于当前验收标准但尚未完成的内容包装成“后续优化”。

## 建议 main 重点审查

- `path/to/file:Symbol`：{{复杂、敏感或容易回归的原因。}}
- {{计划偏移、边界条件或人工验收重点。}}

## 返工记录

### Round {{N}}

- 对应整改：`findings.md#{{finding-id}}`
- 修改内容：{{文件、符号和行为。}}
- 验证证据：{{测试和 CI。}}
- 尚未解决：{{无 / finding ID 与原因。}}
