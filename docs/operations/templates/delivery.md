# 交付报告：{{任务名称}}

> 本文件描述一个冻结的 PR 交付候选，而不是复述原计划。执行 session 在该候选通过必需 CI 后更新本文件并暂停；main 的每轮验收和最终收尾也写在本文件中。任何新 push 都会使旧候选和旧验收结论失效。

## 交付状态

- 结果：{{等待验收 / 阻塞 / 失败 / 部分完成 / 放弃}}
- PR：{{编号与 URL / 尚未创建（早期阻塞）}}
- 分支：`{{branch}}`
- PR base：`main` @ `{{完整 base SHA / 尚未冻结（早期阻塞）}}`
- 交付候选 head：`{{完整 PR head SHA；等待验收或返工候选必填，早期阻塞写“尚未提交”}}`
- 规划提交：`{{execution.md / 计划材料所在完整 SHA / 尚未提交}}`
- `ci-gate`：{{通过 / 未通过 / 阻塞 / 未触发（早期阻塞）}}，{{最新候选对应的运行 URL / 不适用及原因}}
- 其他必需检查：{{无 / 按范围跳过的 job 与原因}}
- 交付时间：{{ISO 8601 或日期时间}}
- 执行 session：{{已暂停 / 尚未暂停及原因}}

若结果不是“等待验收”，应在“未完成项与阻塞”中说明当前可保留成果和 main 需要决定的事项。

等待验收或返工交付必须填完整候选 head 和对应 CI；早期阻塞可以在 PR 尚未创建、没有可提交改动或 CI 尚未触发时更新本文件，不得编造 SHA、运行链接或空提交。

## 阻塞快照

仅在结果为“阻塞”“失败”“部分完成”或“放弃”时填写；等待验收时写“无”。

- 证据：{{失败日志、复现步骤、合同冲突或环境状态。}}
- 最后安全提交：`{{HEAD / 无}}`
- 工作树状态：{{干净 / 未提交内容及其处置。}}
- 受影响的 AC/范围：{{无 / AC-XX 与影响。}}
- 需要的决定：{{谁需要决定什么；可给可行选项。}}
- 恢复条件：{{满足什么后可以恢复施工。}}

## 实现摘要

### 用户可见结果

- {{用户现在可以观察到什么变化。}}

### 内部实现

- {{核心机制、数据流、状态变化或接口行为。}}

## 验收标准对应

| 标准 | 结果 | 证据 |
|---|---|---|
| AC-01 {{名称}} | {{通过 / 未通过 / 待人工验证}} | {{测试、CI、人工步骤或代码位置}} |
| AC-02 {{名称}} |  |  |

不得把未运行的验证写成通过。需要 main 或用户完成的人工验收应明确标注。

## 主要代码位置

| 文件或符号 | 变更 | 设计原因 |
|---|---|---|
| `path/to/file:Symbol` | {{做了什么}} | {{为什么在这里实现}} |

列出理解实现所需的关键位置，不需要逐项枚举机械生成文件或完整 diff。

## 与计划的偏移

- {{无。}}
- {{若有：原计划、实际实现、改变原因、影响、main 是否需要确认。}}

执行中产生但未写入原方案的重要技术决定也记录在这里，并指出是否已经同步到现行规范或设计文档。

## 验证结果

### 本地检查

| 命令 | 结果 | 说明 |
|---|---|---|
| `git diff --check origin/main...HEAD` | {{通过 / 失败}} | 检查本分支已提交变更 |
| `{{命令}}` |  |  |

### GitHub CI 与编译

| Workflow / Job | 结果 | 链接或说明 |
|---|---|---|
| `ci-gate` | {{通过 / 未通过}} | {{URL}} |
| {{相关编译或构建}} | {{通过 / 按范围跳过 / 不适用}} |  |

### 人工验证

- {{无 / 环境、步骤、结果、脱敏证据。}}

## 测试、文档与合同

- 测试：{{新增或修改的位置；没有则解释为什么已有覆盖足够。}}
- 现行文档：{{更新位置 / 不适用。}}
- 类型或机器合同：{{更新位置 / 不适用。}}
- 迁移或发布说明：{{更新位置 / 不适用。}}

## 兼容性、风险与回滚

- 兼容性：{{无影响 / 具体影响和处理。}}
- 数据与配置：{{无影响 / 迁移、默认值、回退。}}
- 安全与隐私：{{无影响 / 边界变化。}}
- 回滚方式：{{回退提交即可 / 需要额外步骤。}}
- 剩余风险：{{无 / 具体风险、触发条件和缓解方式。}}

## 未完成项与阻塞

- {{无。}}
- {{未完成内容、原因、当前状态、需要 main/用户做出的决定。}}

不得把明确属于当前验收标准但尚未完成的内容包装成“后续优化”。

## 建议 main 重点审查

- `path/to/file:Symbol`：{{复杂、敏感或容易回归的原因。}}
- {{计划偏移、边界条件或人工验收重点。}}

## main 验收记录

> 仅 main 填写。main 不需要创建额外审批表；每轮结论都绑定本节列出的冻结候选。新提交后旧记录保留为历史，但不再适用于新的 head。

### Round {{N}}

- 结论：{{通过 / 需要整改 / 阻塞}}
- 审查范围：{{完整任务 / 指定模块 / findings 复验}}
- 审查候选 head：`{{完整 PR head SHA}}`
- `ci-gate`：{{通过 / 未通过 / 阻塞}}，{{URL}}
- AC 与人工验证：{{通过情况、未完成项、证据。}}
- 接受的偏移或风险：{{无 / 内容、理由和责任归属。}}
- 结论与证据：{{PR diff、代码位置、复现或链接。}}
- 日期：{{日期时间}}

## main 收尾

> 仅 main 填写。有功能 PR 时在其合并后完成收尾；没有功能 PR 的失败、放弃、部分完成或阻塞也在这里如实记录，并通过只包含记录的收尾 PR 持久化。阻塞任务保持活动，不归档。`task.json.status=completed` 仅说明 Trellis 目录已归档，不能代替这里的真实最终结果。

- 最终结果：{{完成 / 部分完成 / 失败 / 阻塞 / 放弃}}
- 功能 PR 与验收候选：{{PR URL；完整 head SHA / 无功能 PR及原因}}
- main 合并提交：`{{功能 merge commit / 无功能 PR或未合并及原因}}`
- 收尾记录 PR：{{URL 与提交 / 尚未创建及原因}}
- 知识库与合同：{{随功能 PR 更新的位置 / 收尾 PR 更新的位置 / 不适用及判断。}}
- PENDING 去向：{{无 / ID、完成证据、剩余范围或用户放弃决定。}}
- 归档：{{任务路径、`archive --no-commit` 与 `validate --all` 结果、归档 PR / 阻塞而保持活动及原因。}}
- worktree 与分支清理：{{路径、清理结果；本地/远端分支状态 / 保持未清理及原因。}}
- 遗留风险：{{无 / 可保留成果、后续入口和责任人。}}

## 返工记录

### Round {{N}}

- 对应整改：`findings.md` 中的 `{{finding-id}}`（例如 `F-001`；不要依赖会随标题变化的 Markdown 锚点）
- 返工候选 head：`{{完整 PR head SHA}}`
- `ci-gate`：{{通过 / 未通过 / 阻塞}}，{{URL}}
- 修改内容：{{文件、符号和行为。}}
- 验证证据：{{测试和 CI。}}
- 尚未解决：{{无 / finding ID 与原因。}}
