# 施工入口：{{任务名称}}

> 先调用 `$gkd-execute`。活动状态以 `python3 .trellis/scripts/task.py status {{task}}` 为准；本文件只保存任务特有边界。

## 权威材料

1. `AGENTS.md`
2. `.trellis/tasks/{{task}}/prd.md`
3. {{`design.md` / 无}}
4. {{`implement.md` / 无}}
5. `docs/operations/multi-worktree/execution-and-delivery.md`
6. {{本任务需要的现行规范、研究或代码入口}}
7. {{返工时：`findings.md` 的未解决 ID / 首次实施}}

- 实施授权：{{已确认；日期和覆盖范围}}
- PENDING：{{无 / ID 与处理决定}}
- 依赖：{{无 / 任务路径、接口和合并顺序}}

材料冲突或实施授权不明确时停止并报告 main，不用本摘要覆盖 PRD、设计或现行合同。

## 锁定边界

- {{用户/main 已确定且执行 session 不得改变的决定。}}
- {{必须保持的兼容性、接口、数据或安全行为。}}
- {{没有额外边界时写“以 PRD 和现行合同为准”。}}

## 实现自由度

- {{可根据当前代码选择的实现细节。}}
- {{需要在 delivery.md 解释的取舍。}}

## 范围

### 必须完成

- {{功能、修复、测试、文档或迁移；用 PRD/implement 步骤 ID 引用。}}

### 允许修改

- `path/or/module`：{{原因和边界}}

### 范围外

- {{不处理的相邻问题、重构或产品行为。}}

### 并行冲突

- {{无 / 共享文件、接口、语义边界和预期顺序。}}

## AC 与证据入口

| AC | 执行结果入口 | 需要的证据 |
|---|---|---|
| AC-01 | `path/to/file:Symbol` | {{合同、测试、CI 或人工步骤}} |
| AC-02 |  |  |

完整可观察结果只写在 `prd.md`，这里不复制 Given/When/Then。

## 验证

- 本地允许：{{命令与验证内容 / 无}}
- GitHub：{{`ci-gate`、`pr-title`、相关编译或候选构建}}
- 人工/环境：{{无 / 步骤、环境、责任人和脱敏证据}}

不得运行 `AGENTS.md` 禁止的依赖安装、构建或测试。未运行项必须在 `delivery.md` 说明原因。

## 任务特有停止条件

- {{必须修改范围外的重要模块或公共接口。}}
- {{锁定决定与代码/合同冲突，或无法满足 AC。}}
- {{依赖、兼容性、迁移、安全、真实凭据或高风险操作需要新决定。}}
- {{CI/环境失败无法证明属于本任务且没有可靠任务内修法。}}

通用阻塞、交付和恢复命令见执行专题。停止时先持久化证据和恢复条件，再暂停。

## 当前返工

- 未解决 finding：{{无 / F-001、F-003}}
- 本轮只处理：{{首次实施 / 指定 finding}}
- 保持不变：{{无 / main 指定边界}}
