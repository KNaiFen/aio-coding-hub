# GKD 验收与收尾：{{任务名称}}

- 结果：{{完成 / 部分完成 / 失败 / 阻塞 / 放弃}}
- 被验收 head：`{{完整 SHA / 无}}`
- `gkd_acceptor` 结论：{{通过 / 阻塞 / 拒绝}}
- 必需 CI：{{`ci-gate`、`pr-title` 及相关检查}}
- merge commit：`{{完整 SHA / 未合并及原因}}`
- AC 与偏移：{{逐项结果}}
- 知识库 / PENDING：{{路径与去向}}
- 归档与清理：{{bundle closeout、worktree、分支和 runtime 结果}}

所有字段必须来自固定 head、GitHub 实况或 bundle receipt，不得编造状态。
