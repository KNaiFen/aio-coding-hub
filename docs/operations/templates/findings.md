# 验收整改意见：{{任务名称}}

> main 负责写问题、证据和复验要求；执行 session 不删除或改写原始意见，只在“执行回应”中说明整改和证据。

## 当前结论

- 结论：{{需要整改 / 阻塞}}
- PR：{{URL}}
- 审查轮次：{{Round N}}
- 审查版本：`{{PR head}}`
- CI 状态：{{实时结果和相关链接}}
- 审查范围：{{完整任务 / 指定模块 / 上轮 findings 复验}}

## 总结

{{用几句话说明不能合并的核心原因、影响范围和本轮整改重点。}}

## 未解决问题

- [ ] F-001 {{简短标题}}
- [ ] F-002 {{简短标题}}

按严重性排序。顶部清单以每项最新“main 复验”状态为准，finding ID 永不复用；纯建议写入“建议项”，不要放进必须整改清单。

每个 finding 都必须填写“返工责任”。若责任为 `main-direct-fix`，还必须记录接管时间、接管原因、修改范围、预期与实际 `change-scope` 字段（`scope`、`full_ci`、`frontend_ci`、`rust_ci`、`shared_ci`、`docs_checks`）、选中/跳过的 jobs、保持不变的行为，以及实际 scope 选中长任务时交回执行 session 的处理。

## Round {{N}}

- 本轮整改候选 head：`{{完整 PR head SHA；首次写验收时的 head}}`
- `ci-gate`：{{通过 / 未通过 / 阻塞}}，{{URL}}
- 本轮范围：{{首次完整验收 / 指定 findings 复验}}

### F-001：{{问题标题}}

- 严重程度：{{阻塞 / 重要 / 一般}}
- 返工责任：{{执行 session / main-direct-fix}}
- 对应要求：{{AC、PRD、设计决定、现行合同或质量风险}}
- 证据：`path/to/file:line`、`Symbol`、失败测试、CI 日志或复现步骤
- 当前行为：{{准确描述实际行为。}}
- 影响：{{用户影响、错误结果、回归、安全或维护风险。}}
- 必须达到的结果：{{描述修复后的可观察行为，不在有多种合理方案时强行指定代码写法。}}
- 修改边界：{{允许涉及的模块，以及必须保持不变的行为。}}
- 测试和文档：{{需要新增或更新的验证、合同和文档。}}
- 复验方式：{{main 如何判断这一项已解决。}}

**执行回应**

- 修改：
- 代码位置：
- 验证：
- 计划偏移：

执行 session 只能补充回应，不删除或改写 main 的原始问题。main 写入本文件后，执行 session 恢复施工前先保留该文件；再次交付时它必须随任务分支的提交保留在 PR 中。

**main 复验**

- 状态：{{待复验 / 已解决 / 未解决 / 重新打开 / 接受风险}}
- 复验候选 head：`{{完整 PR head SHA}}`
- `ci-gate`：{{通过 / 未通过 / 阻塞}}，{{URL}}
- 结论与证据：
- 日期：

**main-direct-fix 补充记录（仅责任为 `main-direct-fix` 时填写）**

- 接管时间、冻结 head、工作树状态和未提交内容归属：
- 接管原因与修改范围：
- 预期/实际 `change-scope` 字段（`scope`、`full_ci`、`frontend_ci`、`rust_ci`、`shared_ci`、`docs_checks`）：
- 选中/跳过的 jobs：
- 若实际 scope 选中长任务，交回执行 session 的时间、原因、最后安全 head、恢复条件和通知证据：

### F-002：{{问题标题}}

- 严重程度：
- 返工责任：{{执行 session / main-direct-fix}}
- 对应要求：
- 证据：
- 当前行为：
- 影响：
- 必须达到的结果：
- 修改边界：
- 测试和文档：
- 复验方式：

**执行回应**

- 修改：
- 代码位置：
- 验证：
- 计划偏移：

**main 复验**

- 状态：待复验
- 复验候选 head：
- `ci-gate`：
- 结论与证据：
- 日期：

**main-direct-fix 补充记录（仅责任为 `main-direct-fix` 时填写）**

- 接管时间、冻结 head、工作树状态和未提交内容归属：
- 接管原因与修改范围：
- 预期/实际 `change-scope` 字段（`scope`、`full_ci`、`frontend_ci`、`rust_ci`、`shared_ci`、`docs_checks`）：
- 选中/跳过的 jobs：
- 若实际 scope 选中长任务，交回执行 session 的时间、原因、最后安全 head、恢复条件和通知证据：

## CI、编译或环境问题

| 检查 | 状态 | 证据 | 期望处理 |
|---|---|---|---|
| {{job}} | {{失败 / 阻塞}} | {{URL 或日志摘要}} | {{任务内修复 / 交 main 判断}} |

没有则写“无”。不要只粘贴日志；说明失败与本任务的关系和预期结果。

## 计划偏移需要处理

- {{无。}}
- {{偏移内容、为什么不可接受、应恢复原方案还是由 main/用户重新决策。}}

## 本轮返工边界

### 必须处理

- F-001
- F-002

### 不要顺带处理

- {{与本轮无关的重构、功能或相邻问题。}}

### 可以自主决定

- {{执行 session 可自行选择的实现细节。}}

## 再次交付要求

- [ ] 所有必须整改项都有执行回应和代码证据。
- [ ] `delivery.md` 已更新实现、偏移、验证和返工记录。
- [ ] 新提交已推送，PR 最新 head 的必需 CI 和相关编译为绿色。
- [ ] 本轮等待验收或返工候选的完整 PR head SHA 和对应 `ci-gate` 已写入 `delivery.md` 与本文件；早期阻塞已明确写出未触发原因。
- [ ] 没有把当前范围内未完成内容改名为“后续优化”。
- [ ] 执行 session 已暂停并通知 main 复验。

## 建议项

- {{不阻止本次合并，但值得未来考虑的内容；没有则写“无”。}}
