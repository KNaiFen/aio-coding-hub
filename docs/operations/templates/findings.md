# 验收整改：{{任务名称}}

> main 写原始问题和复验；执行 session 只补“执行回应”。活动候选以 GitHub PR 为准。

## 当前轮次

- 结论：{{需要整改 / 阻塞}}
- PR：{{URL}}
- 审查锚点：{{绑定被审查提交的 GitHub review/comment URL}}
- 审查范围：{{完整任务 / 指定模块 / findings 复验}}
- 未解决：{{F-001、F-003}}

纯建议放在文末，不进入未解决清单。finding ID 永不复用；已解决历史保留，但执行入口只引用当前未解决 ID。

## Finding 模板

为每个问题复制本节。

### F-{{NNN}}：{{标题}}

- 状态：{{待整改 / 待复验 / 已解决 / 未解决 / 重新打开 / 接受风险}}
- 严重程度：{{阻塞 / 重要 / 一般}}
- 返工责任：{{执行 session / main-direct-fix}}
- 对应要求：{{AC、PRD、设计、现行合同或质量风险}}
- 证据：`path/to/file:line`、`Symbol`、失败测试、CI 日志或复现步骤
- 当前行为与影响：{{为什么不能合并。}}
- 必须达到的结果：{{修复后的可观察行为。}}
- 修改边界：{{允许涉及的模块和必须保持不变的行为。}}
- 测试与文档：{{需要新增或更新的证据。}}
- 复验方式：{{main 如何判定。}}

**执行回应**

- 修改：
- 代码位置：
- 验证：
- 计划偏移：

**main 复验**

- GitHub 审查锚点：{{绑定复验提交的 review/comment URL}}
- 结论与证据：
- 日期：

**main-direct-fix 接管（仅适用时）**

- 接管时间、冻结 head、工作树状态和内容归属：
- 接管原因、范围和保持不变的行为：
- 预期/实际 `change-scope`：`scope`、`full_ci`、`frontend_ci`、`rust_ci`、`shared_ci`、`docs_checks`
- 选中/跳过 jobs：
- 意外选中长任务时的交回时间、最后安全 head、恢复条件和通知证据：

## 本轮边界

- 必须处理：{{finding IDs}}
- 不要顺带处理：{{相邻重构或功能}}
- 可以自主决定：{{实现细节}}

再次交付前，所有必须整改项都要有执行回应和证据，`delivery.md` 要反映实际变化；执行 session 重新运行 `task.py deliver`、推送并等待最终 head 的适用 CI 后暂停。

## CI 或环境问题

| 检查 | 证据 | 与本任务关系 | 期望处理 |
|---|---|---|---|
| {{job}} | {{URL/摘要}} | {{相关/未知/无关}} | {{任务内修复 / main 决定}} |

没有则写“无”。

## 建议项

- {{不阻止合并的建议 / 无}}
