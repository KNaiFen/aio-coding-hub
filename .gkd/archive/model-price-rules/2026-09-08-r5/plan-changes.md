> 归档快照：2026-09-08 12:29 +08:00。来源：主工作树，作者 main。保留原记录日期、观察时点及历史结论；本机绝对路径与运行时角色标识已脱敏。下文的活动路径是原始来源说明，五类完整记录均已保存于本目录。

# 模型自定义定价与倍率：计划变更

## r5 / 2026-09-08

- 依据：用户明确授权“允许：推送，PR，合并，发版”。
- 交付：允许推送任务分支、普通 PR 和必要 CI 修正、检查通过后 squash 合并及发布 `0.60.60`。远端 main 不直接推送，已有本地主线分叉保留。
- 范围：仅新增发布所需版本元数据及 CHANGELOG；实现行为、单表单互斥、历史隔离和业务 AC 均不变。主代理直接处理局部版本准备；CI 仍全在 GitHub Actions。
- 终点：PR 必要检查通过并合入，主线 CI 生成对应提交的唯一候选，经现有 release 工作流发布；脱敏归档并整理任务活动记录。
- 清理：保留本地及远端任务分支与 worktree；不扩大为删除现场，也不清理其他任务。

## r4 / 2026-09-08

- 依据：用户明确要求“走automatic自动模式”。
- 路线：由 `delegated/manual` 改为 `delegated/automatic`，复用已创建的 `feat/model-price-rules` 分支和执行 worktree。
- 编排：main 启动唯一 `gkd_execute`，完成后接独立 `gkd_accept`，处理 findings 并按 GKD 衔接获准收尾。
- 产品和验证：自定义单价、单表单倍率互斥、历史费用保留、修改范围及零产物/云端测试要求均不变。
- 授权：本轮已批准自动执行，无需再次确认；远端写入、合并、发布和删除许可未扩大。完成具体实现与代码审查后再处理所需云端交付许可。

## r3 / 2026-09-08

- 依据：用户要求只做检测，不增加模式切换或多套面板，不允许同一模型同时设置整体和分项倍率。
- 交互：同一表单显示整体和分项倍率；两边同时有值时提示冲突并阻止保存，保留输入供修改，清空任一侧后解除冲突。
- 数据：删除模式字段/枚举，以可选整体倍率及可选分项倍率字段保存；前后端按有无值执行互斥校验，0 和 1 均算已设置。
- 消融：移除模式控件、多面板和自动清空联动，修订验收用例；固定单价与历史费用约定保持不变。
- 授权：本轮仅修订计划，未创建执行现场或修改业务代码。

### r3 执行批准

- 用户于 2026-09-08 回复“可以，开始执行当前 PLAN”，批准当前 r3，行为和范围未变。
- 已更新 `origin/main`，以 `b147ced0f8cab342ee57925a5bd6b08d37e061db` 创建 `feat/model-price-rules` 分支及同级 `model-price-rules` worktree。
- 本阶段遵循已批准 `delegated/manual` 路线，生成 `.gkd/execution.md` 供手动启动施工；施工拥有范围内修改、零依赖检查与本地提交许可。
- 远端写入、合并、发布和清理许可没有扩大。主工作树分叉、其他 worktree 和旧归档保持原样。

## r2 / 2026-09-08（模式设计已由 r3 替代）

- 依据：用户明确整体只指单个模型，整体与分项倍率不能同时设置，且不影响历史费用。
- 行为：倍率收敛为默认、整体、分项三种互斥模式；删除全模型默认倍率、分项覆盖整体及整体乘分项的候选方案。
- 历史：新增规则只进入后续实时计费；不重算历史，不让现有历史缺失费用补齐套用新规则，已完成 trace 再次落库也保留历史费用。
- 范围：补齐 IPC 注册与设置接线文件；零价支持收敛到请求费用转换、排行榜覆盖计数和金额返回路径。无需历史规则版本表或重算任务。
- 验收：以整体 0.5、分项仅输出 2 的互斥场景分别验算，并增加模式切换无残留、后端拒绝混合模式与历史费用隔离要求。
- 授权：用户本轮是需求澄清，尚未批准施工；r2 保持项目默认手动交接路线，未创建分支/worktree、启动角色或修改业务代码。

## r5 收尾期间清理授权补充 / 2026-09-08

- 来源：用户在本次收尾期间明确要求“为什么要保留，合并了就清理干净”，main 随即交接该决定。
- 此授权替代先前保留任务分支及执行 worktree 的限制；原 PLAN 快照保留原观察时点。
- 本任务成功 squash 合并、必要发布完成、全部事实已保存且无未保存变化后，允许精确删除远端及本地 `feat/model-price-rules`、本任务执行 worktree，以及已归档的五类独占活动记录。
- 允许清理本任务 PR 描述临时文件和已应用的云端修正补丁；不清理其他任务、旧归档或未知现场。
- 本地 main 的既有分叉仍保留，禁止 reset 或将 squash 前任务分支合入本地 main。
