# 供应商周期限额手动重设计划变更

## r3 执行中的验证修复，2026-09-10

- CI run 34479239040/1 的现有依赖审计阻止前端验证，js-yaml override 4.3.1 需要修复版本 4.3.2。main 核对官方公告 `https://github.com/advisories/GHSA-2883-xcg3-v3hh` 和 npm 精确版本元数据，依赖仍只有 argparse ^2.0.1；允许仅在 pnpm-workspace.yaml/pnpm-lock.yaml 升级该 patch 版本并替换原有成熟期例外的精确版本。未加依赖、未安装、未放宽审计。
- 这是完成已批准云端验证的必要最小修复，沿用用户“可以，开始吧”的实现、真实CI失败修复及交付许可；功能语义、验收和交付终点不变。后续 frozen install/audit 与全部既有验证必须成功。
- 独立验收遗漏已记录 review：provider upsert 后限额缓存刷新、fixed daily DST end 和连续四周期快照验证。e2 补齐原获批行为，新增允许修改 src/query/providers.ts 的对应保存联动与已有 tests，不另扩产品范围。

## r3，2026-09-10

- 执行批准补记: 用户回复“可以，开始吧”，承接 main 关于实施、云端验证、独立审查后合并 main 的确认，r3 全部约定动作获批。已创建 `../provider-limit-reset` / `feat/provider-limit-reset`，baseline 为 `25b4203165e709f9c9b0d8f18c0290b12c296b4a`，execution revision e1。
- 用户依据: 用户明确回答后续周期沿新起点递推为“是”，并强调“周限额重置是不影响月限额的，几个限额的重置互不影响”。
- 决定: 关闭 r2 的后续周期待确认项；以各周期自己的重设时间持续推导下一周期。四个周期的起止时间、累计用量和重设标记完全独立，总累计和历史费用保留。
- 实现与验证影响: 写入严格限定 `(provider_id, period)`，禁止供应商共用清零基准；增加固定时间下逐周期比较和“先月后周”的隔离验收，覆盖月限额仍超限时继续拦截。沿用 r2 文件范围与执行路线，不新增接口、组件或抽象。
- 授权: 本轮确认产品行为并授权更新计划；尚未收到实施与交付批准。当前 PLAN r3 已无产品行为待确认项，执行授权仍待确认。
- 交接: 尚无执行工作树或角色，未来 execution 使用 r3；归档目录随当前 revision 更新为 `2026-09-10-r3`。

## r2，2026-09-10

- 原因与用户依据: 用户明确重设同时包含“周期内的累计清零 + 重设周期”，给出周窗口 9/1–9/8 在 9/7 重设后应变为 9/7–9/14，并要求实时生效、无需重启。
- 修正: 删除 r1 中日/周/月继续沿用原自然周期结束点的方案。四种周期均从操作时刻重新起算；将事务提交后同一运行实例立刻使用新窗口列为必要验收。重启仅检验持久化。
- 实现影响: 显示与网关共用手动周期边界，按供应商分别聚合周/月范围；IPC 增加真实窗口 end 字段，首页不再自行推算月初，编辑器实时刷新实际起止和用量；明确处理手动日锚点与之后显式修改日调度的关系。
- 范围影响: 增加首页日期渲染、直接类型/preview fixtures、provider 日调度保存联动及对应必要行为验证。四个独立按钮、累计/历史保留、云端验证和交付终点保持不变。
- 待确认: 建议新锚点在后续周期持续有效，月按同日同时刻的日历月递推、月底截到目标月末；已向用户发出确认，尚未将未答复当成许可。
- 授权: 用户已明确纠正本轮语义并授权修订计划；没有开始实施、推送、合并、发布或清理的新增批准。
- 交接状态: 尚无 execution/progress/review、执行 worktree 或执行角色；以后交接必须使用 r2（以及最终确认结果），不能沿用 r1 的自然周期结束规则。
